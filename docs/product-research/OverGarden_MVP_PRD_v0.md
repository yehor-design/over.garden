> # 📌 СКОУП БІЛДА — РІШЕННЯ ОПЕРАТОРА 2026-06-15
> Повний MVP будується НАПРЯМУ; цей PRD — **авторизований скоуп білда** (НЕ «креслення до C2 PASS»). Це СВІДОМЕ заміщення pre-build C2-гейта: асиметричний ризик (будуємо до C2-підтвердження **H1** = готовності журналити) прийнятий оператором — це вибір, не «гейт пройдено». Валідація H1/H4/H6 і чотирьох інваріантів ЗМІЩЕНА на **ЖИВУ ПОСТ-ЛОНЧ перевірку + pivot-тригери**: **H1** (p_journal) — чи журналять реальні юзери; **H4** (Пуб) — publish-rate реальних користувачів; **H6** — organic-K живих сторінок. Kill-критерії **НЕ скасовано** — вони лишаються повноцінною фальсифікацією + pivot, лише міряються живим (`DRIVE2_CANON_v1.md` §4/§6; `ENTRY_DATA_AND_RANKABILITY_SPEC_v0.md` §1; пост-лонч organic-K — `VIRALITY_RESEARCH_FINAL.md` §11 #4). C2: триває паралельно як де-ризик, що ІНФОРМУЄ білд, але НЕ гейтить. Frozen `KILL_CRITERIA_PREREG_v2.md` §0/§8 (build-гейт) НЕ редаговано — ця супрерсія зафіксована рішенням оператора ззовні (дзеркало зміни інструмента H6, `DRIVE2_CANON_v1.md` §4).

> # 📌 РОЗШИРЕННЯ СКОУПУ — РІШЕННЯ ОПЕРАТОРА 2026-06-21 (КРОС-ЮЗЕРНА ЛІНІЯ В v0)
> Крос-юзерний lineage-шар (**S14–S20**: провенанс-атрибуція, claim/confirm, @-хендли, інвайт, граф-родовід, вплив, follow) додано до скоупу v0 — **НЕ** відкладено у фазу-2. Розглянуту альтернативу (**base-first**: збудувати single-player S1–S13 першим, зміряти H1/H4/H6 живим, додати S14–S20 лише якщо база виживає) **свідомо відхилено**. Прийнятий асиметричний ризик (та сама логіка, що банер 2026-06-15): місяці соло-білду важкого шару, чия мережева цінність компаундна й матеріалізується лише якщо база виживає; новий STATED-ONLY-невідомий — **крос-Пуб** (чи аматор атрибутує походження) — додається до невиміряних p_journal/p_pay, міряється ЖИВИМ. Метрики крос-шару — **WATCH-не-kill** (`OverGarden_B2_METRICS_v0.md §2.В`); kill-критерії незмінні — **H1/H4/H6**. Інваріанти INV1–4 — тверді safety-AC (§5). Frozen `KILL_CRITERIA_PREREG_v2.md` не редаговано.

# OverGarden_MVP_PRD_v0.md — консолідований скоуп-документ MVP (v0)

**Тип:** PRD-консолідація вже-скоуплених джерел в один дисциплінований скоуп-документ. **НЕ генерує нових фіч** — усе підняте з джерел зі збереженням їхніх MVP/post-MVP-тегів і C2-прив'язок. **Дата:** 2026-06-14. **Світ замкнений:** лише файли; брак → `[потрібен джерельний інпут]`.
**Джерела:** `MVP_LOGGING_DESIGN-BRIEF.md` (§2 принципи, §4 build-патерни, §5 двошарова модель, §6 поза-MVP, §7 відкриті-C2) · `MATCHING-ENGINE_STACK_SPEC.md` (§4 стек, §6 компоненти, §10 межа MVP/post-MVP, §11 PoC, §12 build-прогалини) · `DB_SEED_AND_DATA-MODEL_SPEC_v1_2.md` (§2 режим, §4 шари, §7 модель, §8 матчинг-шар F, §9 сід) · `Бізнес-ідея_v2.txt` (§0.1 A/C/D, §11.1 п.31/32 IN, §11.2 OUT).
**FREEZE-LIST (READ-ONLY — лише посилаюся, НЕ змінюю):** `KILL_CRITERIA_PREREG_v2.md`, `C2_RECRUITING_AND_SCREENER_v4.md`, `C2_INTERVIEW_GUIDE_v2.md`, `ЦА_CANON_v1.md`, `POSITIONING_CANON_v1.md`, `DRIVE2_CANON_v1.md`. [SYNCED 2026-06-23 — повний заморожений набір 6 файлів]

---

## CHANGE-SET (ADD / REPLACE / PRESERVE / FLAG)

- **CHANGE-SET 2026-06-21 — ЗМІНА ПРИМІТИВУ ЛОГУВАННЯ (рішення оператора).** Одиниця логу: атомарна подія → НАРАТИВНИЙ ЕПІЗОД. Обов'язкові поля запису: title + body. Тип події та milestone-чекбокс прибрано повністю. Дворівнева прив'язка запису (об'єкт / простір). Сезонні prompt-моменти й мітигація «білого листа» — свідомо ВІДКЛАДЕНІ (post-MVP). Наслідки — у S1, S3, S4, S6, AC-S1, §6, §7, §8 нижче. *(Наявний CHANGE-SET нижче — PRESERVE, не видаляється.)*
- **CHANGE-SET 2026-06-21 — КРОС-ЮЗЕРНИЙ ЛІНІЙНИЙ ШАР (рішення оператора, повний скоуп).** Додано S14–S20 (провенанс-атрибуція, claim/confirm, @-хендл, інвайт не-юзера, лінійний граф+influence, ask-the-lineage, follow) + US/AC на кожну + 4 приватнісні інваріанти як ТВЕРДІ safety-AC (AC-INV1–4); оновлено §3/§6/§7. Джерела: `LINEAGE_SOCIAL_GRAPH_SPEC_v0.md`, `CROSS_USER_TRUST_AND_PRIVACY_SPEC_v0.md`, `DB_SEED §7.9/§7.10`, `ENTRY §2/§3`. Лінія = ров/утримання (`VIRALITY §6`), НЕ K>1; крос-юзерні поведінки — STATED-ONLY, живі пост-лонч, WATCH не kill.
- **CHANGE-SET 2026-06-22 — PUBLIC-ONLY РІШЕННЯ + GDPR-МЕХАНІЗМИ (рішення оператора).** Конфлікт public-only ↔ BG-private-by-default закрито → public-only (ризики прийняті). AC-S9a анотовано; додано тверді AC-S9c (розкриття при першій публікації) + AC-S9d (видалення→де-індексація). Джерело: `CROSS_USER_TRUST §7.1`. Правова підстава Art. 6 — TBD юр-рев'ю до BG/EU (`DB_SEED §13`); механізми потрібні за будь-якої підстави.
- **ADD — цей файл** `OverGarden_MVP_PRD_v0.md`: новий консолідований PRD-скоуп. Нічого не генерує понад джерела.
- **PRESERVE (дослівно теги/прив'язки) — усі джерела:** MVP/post-MVP/фаза-1-теги й `прототип-кандидат`/`гіпотеза — C2 #N` підняті як є (`MVP_LOGGING_DESIGN-BRIEF.md` §4/§6; `MATCHING-ENGINE_STACK_SPEC.md` §10; `Бізнес-ідея_v2.txt` §11.1/§11.2).
- **PRESERVE (вказівник, БЕЗ правок) — FREEZE-LIST:** PRD цитує `KILL_CRITERIA_PREREG_v2.md` (H1/H4/H6 як гейт), `C2_RECRUITING_AND_SCREENER_v4.md` (хвиля=садівники), `C2_INTERVIEW_GUIDE_v2.md` (Я-1 публічний-канал, Я-9 read-loop), `ЦА_CANON_v1.md` §5 (скоуп хвилі-1), `DRIVE2_CANON_v1.md` (H6 organic-K жива-перевірка §4/§6; ENTRY data-spec виведена §3) — не змінює жодного порога/клітинки/скоупу. [SYNCED 2026-06-23 — DRIVE2 додано: реально цитується §0/§6/§8]
- **REPLACE — НІЧОГО.** PRD не замінює й не переписує джерел.
- **FLAG 1 (крос-аналіз, несуче):** §6 нижче — наш H4-поріг (kill §6) ТВЕРДІШИЙ гейт, ніж «§19.3 міряти UX»: якщо H4 валить публічний показ, публічність-за-замовчуванням ПЕРЕВЕРТАЄТЬСЯ попри курс-лок §0.1 A.1.
- **FLAG 2 (термінологія offline):** `DB_SEED §2` тепер фіксує локальну копію КАТАЛОГУ для роботи tap-select без мережі; ПРОДУКТ — «online-first + офлайн-ЗАХОПЛЕННЯ, НЕ offline-first» (`Бізнес-ідея_v2.txt` §0.1 D, §11.1 п.32; `TECH_STACK_DECISIONS §2.8`). Це не суперечність: локальна копія каталогу ОБСЛУГОВУЄ офлайн-захоплення, але не тягне CRDT/offline-first архітектуру всього застосунку. Розведено в §2/§3.
- **FLAG 3 [RETIRED 2026-06-21]:** ЗНЯТО — однотап-логування більше не існує (body обов'язковий), таргет «≥~80% один тап» став беззмістовним. *(Первісний текст збережено, не видалено:)* поріг «≥~80% регулярних дій — один тап» (§5/§7) — **операторський дизайн-таргет, не корпусне число**; якісна вимога (більшість регулярних дій — один тап через recents/повтор) є в `design-brief §4`, але сам відсоток `[потрібен джерельний інпут для калібрування]`.
- **FLAG 4 (провенанс-уточнення):** історичні посилання на `DB_SEED …offline-first §54` нормалізовано: у файлі це `DB_SEED §2` (р.54), тепер сформульовано як локальна копія каталогу для offline-capture.
- **CHANGE-SET 2026-06-23 — PAGE-ARCHITECTURE КАНОНІЗАЦІЯ (рішення оператора D1–D8).** Канонізовано вісім закритих page-architecture рішень (повний провенанс — `OverGarden_PAGE_ARCH_DECISIONS_v0.md`, записи D1–D8). **ADD у §2 SCOPE** три нові MVP-фічі: **S21** (акаунт-система й auth-gating — гість-читає-все, акаунт-на-будь-яку-дію-запис, лайки як анонімний виняток, реєстраційна стіна на «Зберегти», 4 методи входу), **S22** (мовна/локаль-модель — три мовні папки `/uk//bg//ru/` на `over.garden`, language-only hreflang, «ринок»-у-runtime, UGC не перекладається) і **S23** (трирівнева модель видалення/архіву/стирання — 410 Gone+де-індексація, recoverable Архів, erasure-on-request однаково UA+BG). **ADD у §5** відповідні **AC-S21/AC-S22/AC-S23**, новий ТВЕРДИЙ приватнісний інваріант **AC-INV5** (справжня незворотна анонімізація-A при стиранні), і **анотовано AC-S9d** [REVISED] (де-індексація інтегрована з трирівневою моделлю S23 + erasure-on-request). Архітектурно УТОЧНЕНО (живе в `B5_SEO_CONTENT_ARCHITECTURE_v2.md` + `OverGarden_PAGE_ARCHITECTURE_v1.md`, окремих S-айтемів НЕ потребує — див. §8): агрегаційний двигун із типованими фасетами + тег-як-резолюція (D2), класифікація поверхонь share-test'ом + редактор-аркуш + список чернеток (D3), профіль і повний граф-родовід публічно-видимі але `noindex` з індексованим провенанс-підсумком (D6), крос-сторінки `noindex`-до-порогу demand-gated (D7), homepage окремо від індексованих ринкових + noindex-кампанійних landings (D8). PRD визнає D2/D3/D6/D7/D8 прийнятими, не дублює їхню специфікацію. Нічого з §3 OUT не виведено; FREEZE-LIST не торкано. Джерело: [PAGE_ARCH_DECISIONS · D1–D8 · рішення-оператора-2026-06-23].

---

## 1. ПРОДУКТ-ОДНОРЯДОК + МЕТА v0

**Однорядок:** OverGarden v0 — особистий пошуковий наративний журнал вирощування по простору/об'єкту з мінімізованим КОМПОЗИЦІЙНИМ тертям (голос→текст, фото-старт) (`Бізнес-ідея_v2.txt` §0.1 A.4–A.5; модель — `DB_SEED §7`).

**Мета v0 = single-player-utility-first:** цінність — власний пошуковий щоденник + навчання, **НЕ** мережа/read-loop на старті (на старті читати нема кого) (`MVP_LOGGING_DESIGN-BRIEF.md` §2). **Read-loop — компаундний ров ФАЗИ-2**, не v0 (`design-brief` §2; узгоджено з `KILL_CRITERIA_PREREG_v2.md` §2 read-loop = окремий сигнал H2). Проектувати під power-ядро + casual-хвіст на самокористі, не MAU/соц-тиск (`design-brief` §2).

---

## 2. SCOPE (v0 IN) — кожна фіча з провенансом і C2-тегом джерела

**S1 · Ядро логування — НАРАТИВНИЙ ЗАПИС у журналі об'єкта або простору.** Обов'язкові поля: title + body (вільний текст / голос→текст). Тип події та milestone-чекбокс ПРИБРАНО повністю. Об'єкт не вводиться полем: запис створюється або ВСЕРЕДИНІ конкретного об'єкта (прив'язка з контексту, без тегування), або на рівні ПРОСТОРУ з посиланням на ≥1 об'єкт ЦЬОГО Ж простору. Дата дефолт=сьогодні; решта (фото, погода, кількість, видимість локації) опційні. (`design-brief` §4 ред.2026-06-21 · `MVP` · `прототип-кандидат`.)

**S2 · Backdating першим класом — кнопка, не edit.** (`design-brief` §4 «Backdating» · `MVP` · `прототип-кандидат`.)

**S3 · [DEPRECATED 2026-06-21]** Recents / повтор останньої дії — мав сенс для атомарних подій; для наративу не застосовний. Кандидат post-MVP: «дописати до історії». (`design-brief` §4 · було `MVP` · `прототип-кандидат`.)

**S4 · Фото — ОПЦІЙНЕ медіа тіла запису** з авто-EXIF (авто-EXIF = лише дата; локація з EXIF НЕ зберігається (inv. location-lock)); alt-текст; сорт/ID опційні; «Unknown → спільнота пізніше». Технічна межа: direct-upload іде тільки в private R2 quarantine; публічно рендериться лише stripped/optimized derivative; оригінал не публічний і видаляється після обробки. [відео — DEFERRED post-MVP, рішення оператора 2026-06-25; фото-only в MVP]. (`design-brief` §4 ред.2026-06-21 · `MVP`; `Бізнес-ідея_v2.txt` §11.1 п.7; `TECH_STACK_DECISIONS` §2.11/§2.13/§3 · рішення-оператора-2026-06-26.) *[C2 #1 «фото primary vs опційне» — ЗНЯТО 2026-06-21: primary = body, фото опційне.]*

**S5 · Progress/winrate-екран на save** (прогрес-бар + зерно «Year in Garden»; «момент радості» на ЛОКАЛЬНОМУ save-екрані, не у фіді). (`design-brief` §4 «Winrate/progress» + §2 «момент радості» · `MVP` · `прототип-кандидат`.)

**S6 · Модель ПРОСТІР → ОБ'ЄКТ → ЖУРНАЛ; картка об'єкта несе контекст.** Сутності каталогу: `Taxon`, `CatalogItem` (сорт/порода), `Name`, `Source` (`DB_SEED §7.1–§7.4`); точка стику з продуктом — `DB_SEED §7.7`. Журнали простору й об'єкта, вільний редактор, чернетки, публікація (`Бізнес-ідея_v2.txt` §11.1 п.1–10). **Дворівнева прив'язка запису (ADD 2026-06-21):** (1) запис-об'єкта — рівно 1 об'єкт, без тегування (прив'язка з контексту); кардинальність 1:M. (2) запис-простору — посилання на ≥1 об'єкт ВИКЛЮЧНО в межах того простору; кардинальність M:N, обмежена простором (усі згадані об'єкти мають належати простору запису).

**S7 · Tap-select сорту з зовнішнього каталогу + user-add + «Unknown» fallback.** Typeahead — первинний шлях вводу (`DB_SEED §8.2`); гейт створення нового запису (`DB_SEED §8.3`); м'яка канонізація популярністю (`DB_SEED §8.5`). (`design-brief` §4 «Зовнішній каталог» · `MVP` · `гіпотеза — C2 #4`.)

**S8 · Матчинг-стек = MVP-мінімум (шар F).** A Meilisearch (пошук/typeahead); B1 ICU + власна крихітна fold-мапа кирилиці; B2 CyrTranslit (трансліт-ключ; translit-ua за потреби); C1 RapidFuzz (гейт «це воно?»); D1 унікальний індекс по канонічній формі (+ Splink/string_grouper можна трохи згодом). (`MATCHING-ENGINE_STACK_SPEC.md` §10 «MVP-мінімум» + §4 стек; `DB_SEED §4` шар F + §8.6 стек · `MVP`.)

**S9 · Модель видимості — ДВІ ОРТОГОНАЛЬНІ ОСІ (не суперечність).**
- Вісь A — **видимість КОНТЕНТУ: публічна за замовчуванням** (SEO/соц-рушій; курс залочив; інтерв'ю міряють UX/сприйняття, не «чи робити»). (`Бізнес-ідея_v2.txt` §0.1 A.1, A.3; §11.1 п.29.)
- Вісь B — **видимість ЛОКАЦІЇ: селектор на простір/об'єкт точно / регіон / приховано; дефолт регіон; точні координати лише server-side, не йдуть на клієнт.** (`Бізнес-ідея_v2.txt` §0.1 C; §11.1 п.31.)
- Підкреслення: це НЕ конфлікт — публічність контенту й гранулярність локації — РІЗНІ осі (узгоджено з `ЦА_CANON_v1.md` §2-стиль розведення осей; `design-brief` §5 «регіональний рівень за дефолтом»).

**S10 · Офлайн-ЗАХОПЛЕННЯ: online-first + локальна черга (запис+фото), синк при мережі.** НЕ offline-first. Черга реалізується як Dexie/IndexedDB + idempotency keys + видимі стани pending/synced/error; background sync на iOS не обіцяється як гарантія. (`Бізнес-ідея_v2.txt` §0.1 D; §11.1 п.32; `TECH_STACK_DECISIONS` §2.8.) Підтримано локальною копією каталогу (`DB_SEED §2`, р.54), щоб tap-select (S7) працював без мережі.

**S11 · Сід-каталог — РОСЛИННИЙ домен (beachhead хвилі-1).** Тварини / бджоли / вино — **НЕ сіяти у v0** (`ЦА_CANON_v1.md` §5 скоуп хвилі-1 = рослинний домен; сід-порядок — `DB_SEED §9.2`, голова — топ-100–200 культур + ручні аліаси для day-1 typeahead). Сід виключає CPVO/UPOV/EURISCO/Genesys/скрейп (`DB_SEED §6.4`).

**S12 · Wishlist «хочу сорт наступного сезону» — додаткова полиця, НЕ основний логувальний примітив.** (`design-brief` §4 «Wishlist» · `MVP` (додаткова полиця) · `прототип-кандидат`.)

**S13 · Решта MVP-сутностей соц/контент-шару (за §11.1 IN, але v0 — single-player-first §1):** теги, категорії, тематичні сторінки, лайки, коментарі (під об'єктами/записами), закладки, підписки, популярність, перелінковка, пошук, редакційний блог, фоновий AI (теги/категоризація/перелінковка/пошук). (`Бізнес-ідея_v2.txt` §11.1 п.11–28.) Нота: ці шари IN за бізнес-планом, але їхня read-loop/мережна ЦІННІСТЬ — фази-2 (§1; `design-brief` §2); у v0 вони існують, але не є первинною ціннісною ставкою. **[REVISED 2026-06-21]** ВИНЯТОК — крос-юзерний ЛІНІЙНИЙ підшар (S14–S20 нижче) ПІДНЯТО до first-class v0 (рішення оператора; `VIRALITY_RESEARCH_FINAL.md §6` = ров/утримання/захищеність, НЕ K>1-virality); «підписки» зі списку формалізовано як `edge_type=follow` (S20). Решта (лайки/коментарі/закладки/блог/популярність/перелінковка/пошук/фоновий-AI) лишається: існує в v0, мережна цінність — фаза-2.

**S14 · Крос-юзерна провенанс-атрибуція.** @-тег у body на ЧУЖИЙ об'єкт/простір/автора → ребро `Edge` (`DB_SEED §7.10`): `edge_type` provenance(«походить від X»)/reference(сорт)/mention(@автор). Typeahead розширено: власні + чужі ПУБЛІЧНІ об'єкти + сорти/каталог + @-автори (`ENTRY §2`). (`LINEAGE §1/§3/§6`; `CROSS_USER_TRUST §1` · `MVP` · `жива-пост-лонч`.)

**S15 · Claim/confirm — згода в ребрі.** Крос-юзерне ребро створюється у `state=proposed`; ціль-таггі підтверджує/відхиляє (→`confirmed`/`declined`); до confirm — нуль публічного внеску. Лінійні soft-confirm, експозиційно-збільшувальні strict. (`LINEAGE §5`; `CROSS_USER_TRUST §1`; інваріант §4.3 → AC-INV3 · `MVP`.)

**S16 · @-хендл-ідентичність.** `User` (`DB_SEED §7.9`) — публічний тегований @-хендл; псевдонімний, без PII; решта профілю за налаштуваннями. (`DB_SEED §7.9`; `CROSS_USER_TRUST §4` · `MVP`.)

**S17 · Інвайт не-юзера.** Тег людини поза платформою → pending-стаб `User` (`status=pending`); `proposed`-ребро НІЧОГО не розкриває (ні автора, ні контенту) до приєднання+claim; стаб→`active` при приєднанні. (`CROSS_USER_TRUST §4` — нуль-витоку · `MVP` · `жива-пост-лонч` — invite-conversion; engineered K>1 структурно малоймовірний, Goel.)

**S18 · Лінійний граф + influence.** Візуалізація родоводу (джерело↔похідні; артефакт «N людей, M країн, одне насіння»); influence = к-сть похідних ліній від тебе. Empty-state-як-запрошення (росток, що росте з даними — `LINEAGE §5`). Рендер сорт-медіаційний + visibility-respect. (`LINEAGE §5/§6`; `CROSS_USER_TRUST §2/§3`; інваріанти §4.1–§4.2 → AC-INV1/2 · `MVP`.)

**S19 · Ask-the-lineage.** Досягнути гроверів у власному провенанс-ланцюзі (спрямований запит у межах лінії); поважає видимість/згоду цілі. (`LINEAGE §5/§7`; `CROSS_USER_TRUST §2` · `MVP` · `жива-пост-лонч` — найспекулятивніша, пріоритет збірки нижчий за S14–S18.)

**S20 · Follow — соц-підписка.** `edge_type=follow` — НАЙНИЖЧА-цінність ребро (`LINEAGE §1`: граф-підписок ≠ граф-провенанс); не збільшує експозицію цілі понад її налаштування; формалізує «підписки» з S13. (`LINEAGE §1/§3`; інваріант §4.2 → AC-INV2 · `MVP`.)

**S21 · Акаунт-система й auth-gating [ADD 2026-06-23].** Стала ідентичність — ОБОВ'ЯЗКОВА (моат-мережа: @-хендли, соцграф, провенанс-атрибуція, follow, claim-inbox, GDPR-видалення вимагають ідентичності). Гість має повний READ-доступ до ВСІХ публічних поверхонь без акаунта; акаунт потрібен на будь-яку дію-ЗАПИС: створення простору/об'єкта/запису, коментарі (топ-рівень і відповіді), закладки, follow, wishlist, @-теги, claim/confirm. ВИНЯТОК — **лайки анонімні** (без акаунта, один-на-пристрій через локальний функціональний токен, тогл, косметичні — НІКОЛИ не годують ранжування/видимість/фід-порядок/популярність, rate-limited). Реєстраційна стіна тригериться на дію **«Зберегти»** (не на «новий запис»); чернетка/стан проноситься крізь auth-флоу. Методи входу: пошта (email/пароль) + Google + Facebook + Apple. (`[PAGE_ARCH_DECISIONS · D1 · рішення-оператора-2026-06-23]`; ідентичність — `DB_SEED §7.9 User` · `MVP`.)

**S22 · Мовна/локаль-модель — двомовні ринки [ADD 2026-06-23].** Три мовні папки на наявному домені `over.garden`: `/uk/` (hreflang `uk`), `/bg/` (hreflang `bg`), `/ru/` (hreflang `ru`). hreflang — **ТІЛЬКИ-МОВНИЙ** (не мова-регіон), бо російська й українська охоплюють обидва ринки, болгарська одно-регіональна. «Ринок» — **runtime, НЕ в URL**: geo-IP/Accept-Language на корені `over.garden/` визначає дефолтну папку + сет перемикача мови (UA-ринок: дефолт українська + російська на вибір; BG-ринок: дефолт болгарська + російська й українська на вибір). Перемикач мови — market-filtered, у хедері й налаштуваннях. UGC **НЕ перекладається** (запис у мові автора); hreflang-кластеризація стосується chrome/агрегацій/едиторіалу, не сирих тіл записів. Каталог готовий: `Name.locale` має `ru`; `CatalogItem.locale_scope` розділяє UA/BG (`DB_SEED §7.2/§7.3`). **[REVISED 2026-06-23] — СУПЕРСЕДУЄ будь-яке попереднє припущення «одна-мова-на-ринок».** (`[PAGE_ARCH_DECISIONS · D4 · рішення-оператора-2026-06-23]` · `MVP`.)

**S23 · Модель видалення / архіву / стирання [ADD 2026-06-23].** Будь-який публічний URL при видаленні віддає **HTTP 410 Gone + де-індексацію** — наскрізний стан кожної індексованої поверхні. Три рівні: **(а)** видалення ЕЛЕМЕНТА (запис/об'єкт/простір/журнал) → приватний **Архів/Кошик**, recoverable, але публічна видимість зникає **НЕГАЙНО** (410); grace до жорсткої чистки; **(б)** видалення АКАУНТА → акаунт закрито, прямі ідентифікатори (email/хендл) відв'язано **НЕГАЙНО**, контент лишається анонімним (моат збережений), UI **ЯВНО** пропонує повне видалення; **(в)** запит на СТИРАННЯ → анонімізація-A (особу й PII геть: обличчя на фото, ім'я в тексті, точна локація, хендл, email; структурні лінійні ребра лишаються анонімними надгробками «походить від колишнього гровера [сорту]»). Erasure — **ЗА ЗАПИТОМ, ОДНАКОВО UA+BG** (без розгалуження по юрисдикції; GDPR не вимагає авто-видалення при close-акаунта, лише виконати запит Art. 17). (`[PAGE_ARCH_DECISIONS · D5 · рішення-оператора-2026-06-23]`; розширює `AC-S9c/d`; `CROSS_USER_TRUST §7` · `MVP`.)

---

## 3. OUT-OF-SCOPE (v0 OUT — явно, щоб не затекло)

- **CV/AI-структурування запису** — post-MVP (`Бізнес-ідея_v2.txt` §0.1 A.4; `design-brief` §6). На MVP — зовнішнє API (Pl@ntNet) або community-ID (`design-brief` §4 CV-рядок).
- **Волонтер-курування каталогу** — post-MVP, фаза 2–3 (`design-brief` §4/§6; `DB_SEED §10.3`).
- **Leaderboards (врожайність / сорт-плейс)** — ризик само-цензури → C2 (`design-brief` §6). Event-бейджі (Untappd-стиль) — НЕ робити (`design-brief` §4).
- **Year-in-Garden / сезонне зведення** — фаза-1 (потребує сезон даних) (`design-brief` §4/§6).
- **Read-loop / мережа як ПЕРВИННА цінність** — фаза-2 ров, не v0 (`design-brief` §2; §1 вище). *[уточнення 2026-06-21: крос-юзерний лінійний шар S14–S20 — це РІВ/захищеність у v0 (`VIRALITY §6`), окремо від read-loop-як-первинна-цінність; первинна ставка лишається p_journal (особистий журнал, H1); споживання-чужого-контенту-як-первинна-цінність — фаза-2.]*
- **Post-MVP-шари матчинг-стеку:** B3 фонетика uk/bg, B4 болгарська клавіатура, B5 повна генерація варіантів, C2 крос-мовний матч (повний конвеєр) (`MATCHING-ENGINE_STACK_SPEC.md` §10 «Post-MVP», §12; `DB_SEED §8.7`, §9.3 шар G post-MVP). НЕ блокують запуск.
- **Тваринний / бджолиний / вино-домен** — не сіяти й не інструментувати у v0 (`ЦА_CANON_v1.md` §5; bee — окремий post-wave трек `C2_INTERVIEW_GUIDE_v2.md` §4).
- **Offline-FIRST** (як архітектура застосунку) — лише офлайн-ЗАХОПЛЕННЯ (`Бізнес-ідея_v2.txt` §0.1 D).
- **eBird «complete checklist» / структурований обхід грядок** — post-MVP, ризик false confidence → C2 #8 (`design-brief` §6).
- **За §11.2 поза MVP (підняте дослівно):** спільноти, особистий блог, Q&A-форум, експертні ролі, reputation, completed seasons, marketplace, курси, LMS, платні консультації, AI-чат/садівник/експерт, AI-діагностика як продукт, обов'язкові поля, типи записів, приватні простори/об'єкти/записи, імпорт фото/старих записів, платні тарифи, freemium (`Бізнес-ідея_v2.txt` §11.2).

---

## 4. USER STORIES (POV аматора-садівника; на кожну IN-фічу)

- **US-S1 (логування) [ред.2026-06-21 під наративну модель]:** Як аматор-садівник, я хочу швидко записати, що сталося (короткий наратив — title+body, голосом або текстом), із картки об'єкта чи простору, щоб зафіксувати історію, не відриваючись від саду. (S1)
- **US-S2 (backdating):** Як аматор-садівник, я хочу окремою кнопкою поставити, що дія була позавчора, щоб журнал лишався правдивим, коли я згадую із запізненням. (S2)
- **US-S3 (recents) [DEPRECATED 2026-06-21 разом із S3]:** Як аматор-садівник, я хочу повторити останню дію по об'єкту, щоб рутинні полив-прополку логувати без повторного вводу. (S3 — для наративу не застосовно; пост-MVP «дописати до історії».)
- **US-S4 (фото):** Як аматор-садівник, я хочу сфотографувати рослину, а сорт/ID додати потім (або лишити «Unknown»), щоб зловити момент і не застрягти на класифікації. (S4)
- **US-S5 (progress):** Як аматор-садівник, я хочу побачити свій прогрес на екрані збереження, щоб відчути миттєву віддачу від запису. (S5)
- **US-S6 (модель):** Як аматор-садівник, я хочу тримати об'єкти в просторах і вести журнал по кожному, щоб моя історія була впорядкована за місцем і рослиною. (S6)
- **US-S7 (tap-select сорту):** Як аматор-садівник, я хочу вибрати сорт із підказки-списку (або додати свій / лишити «Unknown»), щоб не друкувати назву щоразу й щоб мій сорт зв'язався з іншими. (S7, S8)
- **US-S9a (публічність контенту):** Як аматор-садівник, я хочу, щоб мій опублікований запис був видимим (за замовчуванням), щоб мій досвід знаходили й я отримував реакцію/визнання. (S9 вісь A)
- **US-S9b (видимість локації):** Як аматор-садівник, я хочу обрати для простору/об'єкта рівень показу місця (точно/регіон/приховано, дефолт регіон), щоб ділитися результатом, не видаючи, де це фізично. (S9 вісь B)
- **US-S10 (офлайн):** Як аматор-садівник, я хочу записувати й фотографувати без мережі (на дачі/в полі), щоб усе синхронізувалося, коли з'явиться зв'язок. (S10)
- **US-S11 (каталог рослин):** Як аматор-садівник, я хочу вибирати рослинні сорти з готового каталогу, щоб почати вести об'єкт із дня 1. (S11)
- **US-S12 (wishlist):** Як аматор-садівник, я хочу зберегти сорт «спробувати наступного сезону», щоб не загубити план. (S12)
- **US-S14 (провенанс):** Як садівник, я хочу позначити, що мій сорт «походить від» іншого гровера/сорту, щоб зафіксувати справжню історію насіння й знайти «родичів». (S14)
- **US-S15 (claim/confirm):** Як гровер, якого позначили, я хочу підтвердити або відхилити зв'язок, щоб ніхто не приписав мені те, чого не було. (S15)
- **US-S16 (@-хендл):** Як садівник, я хочу публічний @-нік без розкриття особи, щоб мене можна було згадати й знайти мій внесок. (S16)
- **US-S17 (інвайт):** Як садівник, я хочу позначити людину, якої ще немає в застосунку (її насіння), і запросити її — без розкриття чогось до її приєднання. (S17)
- **US-S18 (граф/influence):** Як садівник, я хочу побачити «родовід» свого сорту і скільки ліній пішло від мене, щоб відчути слід, який лишаю. (S18)
- **US-S19 (ask-the-lineage):** Як садівник, я хочу звернутися до гроверів у моїй лінії, поважаючи їхні налаштування. (S19)
- **US-S20 (follow):** Як садівник, я хочу підписатися на цікавого гровера, щоб бачити його нові історії. (S20)

---

## 5. ACCEPTANCE CRITERIA (на кожну стори; тестовані; фрикційні — з названим важелем і C2-тегом)

- **AC-S1 [REPLACE 2026-06-21]:** запис створюється з обов'язковими **title** (непорожній; prefill-дефолт редагований) + **body** (непорожній); без вибору типу події, без milestone-чекбокса; об'єкт прив'язується з контексту (запис-об'єкта, без тегу) АБО запис створюється на рівні простору з посиланням на ≥1 об'єкт того ж простору; дата дефолт=сьогодні, решта полів опційна. *(важіль: композиційне-тертя/активація; `design-brief` §4 ред.2026-06-21; `MVP`.)*
- **AC-S2:** backdating доступний окремою кнопкою першого класу (не через edit існуючого запису); дата змінюється до збереження. *(важіль: точність-без-тертя; `design-brief` §4.)*
- **AC-S3 [DEPRECATED 2026-06-21] (разом із S3):** recents/«повтор останньої дії» для наративу не застосовно; таргет одного тапу знято разом із FLAG 3. *(Первісний текст збережено:)* з картки об'єкта доступні recents/«повтор останньої дії»; ≥~80% регулярних дій логуються одним тапом через quick-chips/повтор-останньої (важіль: звичка/тертя; `design-brief` §4; число — операторський таргет, див. FLAG 3 [RETIRED]).*
- **AC-S4 [REVISED 2026-06-21; ANNOTATED 2026-06-26]:** фото — ОПЦІЙНЕ медіа тіла; зберігається з авто-EXIF (авто-EXIF = лише дата; локація з EXIF НЕ зберігається (inv. location-lock)); alt-текст; сорт/ID — опційні; «Unknown» приймається. Фото проходить pipeline private R2 quarantine → worker re-encode/strip/resize → public derivative; публічна сторінка показує фото **тільки якщо stripped derivative існує**, без fallback на оригінал. Quarantine-оригінал приватний і видаляється після успішної обробки. [відео — DEFERRED post-MVP, рішення оператора 2026-06-25; фото-only в MVP]. *[C2 #1 «фото primary vs опційне» — ЗНЯТО 2026-06-21: primary = body.]* *(важіль: композиційне-тертя/навчання; `design-brief` §4 ред.2026-06-21; `TECH_STACK_DECISIONS` §2.11/§2.13/§3 · рішення-оператора-2026-06-26.)*
- **AC-S5:** після збереження показано локальний progress/winrate-екран (без мережі, без фіду). *(важіль: миттєва-винагорода-на-самокористі; `design-brief` §2/§4.)*
- **AC-S6:** користувач створює необмежені простори → об'єкти → журнали; картка об'єкта показує контекст (паспорт: сорт/регіон/дата за релевантністю). *(`DB_SEED §7`; `Бізнес-ідея_v2.txt` §11.1 п.1–6.)*
- **AC-S7:** typeahead віддає підказку з typo-tolerance (Meilisearch); вибір зі списку — первинний шлях; user-add проходить гейт створення; дублі гасить унікальний індекс по канон-формі. **ПРОВІЗОРНО:** якщо народні/лендрейс-назви домінують — «Unknown» стає основним шляхом (C2 #4). *(`DB_SEED §8.2/§8.3`; `MATCHING §10`; `design-brief` §7 #4.)*
- **AC-S8:** стек шару F = MVP-мінімум §10 (Meilisearch + ICU+fold + CyrTranslit + RapidFuzz + унікальний індекс); Meilisearch — derived index із single worker-owned indexer, не джерело правди; пороги схожості/boosting фіксуються лише ПІСЛЯ обов'язкового PoC на реальних uk/ru/bg-назвах. *(`MATCHING §10 + §11`; `TECH_STACK_DECISIONS` §2.7.)*
- **AC-S9a [ANNOTATED 2026-06-22]:** опублікований контент видимий за замовчуванням; селектор приватності контенту в v0 НЕ вводиться (приватних записів немає — §11.2 п.21). **Рішення оператора 2026-06-22:** конфлікт public-only ↔ BG-private-by-default закрито на користь **public-only** (ризики прийняті); законність public-only-в-ЄС забезпечують AC-S9b (мінімізація локації) + AC-S16 (псевдонімність) + нові **AC-S9c** (розкриття при першій публікації) + **AC-S9d** (видалення→де-індексація) — `CROSS_USER_TRUST §7.1`; правова підстава Art. 6 — TBD юр-рев'ю до BG/EU-лончу (`DB_SEED §13`). *(`Бізнес-ідея_v2.txt` §0.1 A.1; §11.1 п.29; §11.2 п.19–21.)*
- **AC-S9b:** селектор видимості локації — **3 рівні (точно/регіон/приховано), дефолт регіон**; **точні координати не передаються на клієнт** (лише server-side); публічний рендер — за обраним рівнем. *(важіль: довіра/безпека; `Бізнес-ідея_v2.txt` §0.1 C; §11.1 п.31.)*
- **AC-S9c [ADD 2026-06-22 — БІНДЯЧА GDPR-вимога]:** при ПЕРШІЙ публікації (не при реєстрації) — поінформоване розкриття простою мовою («журнал публічний + індексується Google; точне місце ніколи не показується; видалити можна будь-що будь-коли»), БЕЗ передвідміченого чекбокса, явна стверджувальна дія публікації; факт розкриття + версія тексту логуються. Правова підстава Art. 6 — TBD юр-рев'ю (consent / contract 6(1)(b) / LI 6(1)(f); умовність-згоди Art. 7(4)). *(`CROSS_USER_TRUST §7.1`; Art. 13/14 прозорість + Art. 25; флоу — B3; важіль: довіра/легальність.)*
- **AC-S9d [ADD 2026-06-22 — БІНДЯЧА GDPR-вимога]:** видалення публічного запису/об'єкта → (1) сторінка `noindex`, (2) removal-запит до пошуковиків, (3) зняття з sitemap; приховати ≠ видалити для індексованого контенту. *(`CROSS_USER_TRUST §7.1`; Art. 17; де-індексація-механіка — B5; важіль: право-бути-забутим/легальність.)* **[REVISED 2026-06-23 — D5]:** де-індексаційна механіка тепер інтегрована з трирівневою моделлю видалення (S23/AC-S23) та erasure-on-request — будь-який видалений публічний URL віддає **410 Gone** + де-індексацію; де-індексація AI-краулерів/LLM лишається **best-effort** і ЯВНО розкривається в політиці приватності. *(`[PAGE_ARCH_DECISIONS · D5 · рішення-оператора-2026-06-23]`.)*
- **AC-S10:** запис+фото зберігаються без мережі в локальній Dexie/IndexedDB-черзі; синкаються при появі зв'язку через idempotency keys; користувач бачить pending/synced/error; конфлікти версій вирішувані; на iOS Safari UX не залежить від гарантованого background sync. *(важіль: надійність-у-полі; `Бізнес-ідея_v2.txt` §0.1 D; §11.1 п.32; `TECH_STACK_DECISIONS` §2.8.)*
- **AC-S11:** у v0 сід-каталог містить РОСЛИННИЙ домен; тваринний/бджолиний/вино — відсутні; сід без виключених джерел. *(`ЦА_CANON_v1.md` §5; `DB_SEED §9.2/§6.4`.)*
- **AC-S12:** wishlist — окрема полиця «спробувати наступного сезону»; не змішується з логувальним примітивом і не є обов'язковим кроком логу. *(`design-brief` §4.)*
- **AC-S14:** @-тег у body резолвиться в чужий ПУБЛІЧНИЙ об'єкт/простір/автора або сорт; створює `Edge` (`DB_SEED §7.10`) з коректним `edge_type`; крос-юзерний @-тег НЕ змінює якір ВЛАСНОЇ сторінки автора (`ENTRY §3а`). *(`LINEAGE §1/§3`; `CROSS_USER_TRUST §1`.)*
- **AC-S15:** ребро створюється у `state=proposed`; ціль може `confirm`/`decline`; до `confirmed` — нуль публічного внеску/крос-лінку (`ENTRY §3б`); лінійне soft-confirm, експозиційно-збільшувальне strict. *(`CROSS_USER_TRUST §1`.)*
- **AC-S16:** `User.handle` UNIQUE, публічний, псевдонімний (без PII); решта профілю — за налаштуваннями. *(`DB_SEED §7.9`; `CROSS_USER_TRUST §4`.)*
- **AC-S17:** тег не-юзера створює `User` зі `status=pending`; `proposed`-ребро до pending-цілі НЕ розкриває нічого до приєднання+claim; стаб→`active` лише при приєднанні. *(`CROSS_USER_TRUST §4`.)*
- **AC-S18:** граф рендериться з confirmed-ребер; influence = к-сть похідних ліній; публічний рендер сорт-медіаційний + поважає видимість кожного вузла; empty-state = запрошувальний стан, не помилка. *(`LINEAGE §5/§6`; `CROSS_USER_TRUST §2/§3`.)*
- **AC-S19:** ask-the-lineage адресує лише гроверів власного ланцюга; доставка поважає видимість/згоду цілі; не розкриває точну локацію/PII. *(`LINEAGE §5/§7`; `CROSS_USER_TRUST §2`.)*
- **AC-S20:** follow створює `edge_type=follow`; НЕ збільшує видимість/експозицію цілі понад її налаштування; одностороння, без взаємного розкриття. *(`LINEAGE §1`; `CROSS_USER_TRUST §2`.)*
- **AC-S21 [ADD 2026-06-23]:** гість читає всі публічні поверхні без акаунта; перша ж дія-запис (включно з коментарем/follow/закладкою/створенням/claim) вимагає входу; лайк працює анонімно (один-на-пристрій, тогл, косметичний, rate-limited) і НЕ годує ранжування/видимість/популярність; стіна тригериться на «Зберегти» зі збереженням чернетки крізь логін; доступні чотири методи входу (пошта/Google/Facebook/Apple). *(`[PAGE_ARCH_DECISIONS · D1]`; важіль: моат-ідентичність/активація.)*
- **AC-S22 [ADD 2026-06-23]:** публічні сторінки доступні в трьох мовних папках `/uk//bg//ru/` з language-only hreflang; корінь `over.garden/` редіректить за geo у дефолтну папку ринку; перемикач мови показує market-filtered сет (UA: uk, ru; BG: bg, ru, uk); UGC не перекладається. *(`[PAGE_ARCH_DECISIONS · D4]`; важіль: SEO-консолідація/крос-країнна аудиторія.)*
- **AC-S23 [ADD 2026-06-23]:** видалена публічна сторінка віддає **410 Gone + де-індексацію**; видалення елемента йде в recoverable Архів із негайним публічним 410; close-акаунта відв'язує email/хендл і показує явну пропозицію повного стирання; запит на стирання виконується анонімізацією-A у розумний строк (≤30 днів), однаково для UA і BG. *(`[PAGE_ARCH_DECISIONS · D5]`; Art. 17; важіль: право-бути-забутим/легальність.)*

**ТВЕРДІ ПРИВАТНІСНІ ІНВАРІАНТИ (safety-AC — БІНДЯТЬ, НЕ провізорні, НЕ фрикційно-послаблювані; обґрунтування — UA/BG safety-контекст, `CROSS_USER_TRUST §7`):**
- **AC-INV1 (сорт-медіація):** будь-яке ПУБЛІЧНЕ/індексоване крос-юзерне ребро рендериться СОРТ-МЕДІАЦІЙНО («інший гровер [сорту] в [регіоні]»), НІКОЛИ як «сад [користувача] за [локацією]». *(`LINEAGE §4.1`; `CROSS_USER_TRUST §3`.)*
- **AC-INV2 (тег не підвищує видимість цілі):** тег/згадка/follow НІКОЛИ не підвищують видимість цілі понад її ВЛАСНЕ налаштування (локація — `AC-S9b`; контент публічний за дефолтом, Axis A, без приватного селектора у v0). *(`LINEAGE §4.2`; `CROSS_USER_TRUST §2`.)*
- **AC-INV3 (згода-в-ребрі):** крос-юзерне ребро у `proposed`→`confirmed`/`declined`; до confirm — нуль публічного внеску; лінійні soft-confirm, експозиційно-збільшувальні strict. *(`LINEAGE §4.3`; `CROSS_USER_TRUST §1`.)*
- **AC-INV4 (заробляється, не геймиться):** крос-лінки заробляються, не фармляться; анти-геймінг проти масового само-лінкування/реципрок-схем (Google карає link schemes). *(`LINEAGE §4.4`; `CROSS_USER_TRUST §5`.)*
- **AC-INV5 (справжнє стирання — незворотна анонімізація) [ADD 2026-06-23 — ТВЕРДИЙ приватнісний інваріант]:** стирання за запитом застосовує СПРАВЖНЮ анонімізацію (незворотну, без re-identification): прямі ідентифікатори й вбудоване PII видаляються, а структурні лінійні ребра зберігаються анонімними надгробками задля цілісності провенансу інших користувачів; у рідкій зоні, де пара «сорт+грубий регіон» вказує на одну особу, регіон з надгробка прибирається (лишається сам сорт; k-anonymity — `VIRALITY_RESEARCH_FINAL.md` безпека-блок). Достатність анонімізації для Art. 17 підтверджує юр-рев'ю. *(`[PAGE_ARCH_DECISIONS · D5 · рішення-оператора-2026-06-23]`; `CROSS_USER_TRUST §7`.)*

---

## 6. КАРТА C2-ЗАЛЕЖНОСТІ (на фічу: які AC ПРОВІЗОРНІ й чим гейтяться)

- **S4 фото — ОПЦІЙНЕ медіа тіла** → *[C2 #1 RETIRED 2026-06-21: гейт «фото primary vs опційне» мертвий — primary = body, фото опційне за рішенням, не за тестом]* (`design-brief` §7 #1 — знято).
- **S7/S8 стандартизація каталогу сортів** → провізорний AC-S7 → гейт **C2 #4** (`design-brief` §7 #4). Якщо folk/landrace-назви ламають каталог — «Unknown» стає ОСНОВНИМ шляхом, а не fallback.
- **S9a публічний контент + SEO-індексація** → ЖИВА ПОСТ-ЛОНЧ перевірка + pivot: **H4** (прапор Пуб, publish-rate реальних користувачів, `KILL_CRITERIA_PREREG_v2.md` §6) **+ H6** (ранжування UGC=SEO — **жива пост-лонч перевірка organic-K**, `DRIVE2_CANON_v1.md` §4 / `VIRALITY_RESEARCH_FINAL.md` §11 #4; гіпотеза `KILL_CRITERIA_PREREG_v2.md` §7-B). НЕ pre-build гейт (рішення 2026-06-15, банер угорі). Інтерв'ю Я-1 (C2 паралельно, інформує) форсує публічний-vs-приватний канал (`C2_INTERVIEW_GUIDE_v2.md` §5 Я-1).
- **S9 двошаровий соц/SEO (приватний лог × публічна агрегація по сорту-в-локації)** → гейт **C2 #5** (`design-brief` §5/§7 #5): чи цікавий фід ровесників у зоні, чи лише родина/сусіди.
- **S9b зрозумілість/дефолт селектора локації** → гейт **§19.3** (валідаційна агенда, `Бізнес-ідея_v2.txt` §19.3): інтерв'ю+прототип міряють UX селектора.
- **S10 якість офлайн-синку в полі** → гейт **§19.3** (`Бізнес-ідея_v2.txt` §0.1 D/§19.3).
- **S1 утримання наративного логу (p_journal)** *(S3 DEPRECATED 2026-06-21 — прибрано з формули)* → НАСКРІЗНА ЖИВА ПОСТ-ЛОНЧ перевірка **H1** (`KILL_CRITERIA_PREREG_v2.md` §3): жодна фіча логування не «доведена» цим PRD — адопцію знімає ЖИВИЙ пост-лонч сигнал (чи журналять реальні юзери) + pivot; C2 інформує паралельно (`design-brief` §1). НЕ pre-build гейт (банер угорі).
- **S14–S20 крос-юзерний лінійний шар** → НОВІ STATED-ONLY поведінкові невідомі (чи тегують провенанс; claim-rate; invite-conversion; lineage-depth; influence) → ЖИВА ПОСТ-ЛОНЧ перевірка, **WATCH у B2 (НЕ kill-gate)**: `LINEAGE §8` + `VIRALITY §6` — лінія = ров/утримання, НЕ K>1; engineered-virality <0.1% (Goel). Kill лишається **H1/H4/H6** (банер). Пуб(H4) набуває крос-юзерного STATED-ONLY шару — публічність ребер міряється ЖИВИМ, не desk (циркулярність публічного корпусу).

**ЯВНО (несуче — FLAG 1, тепер ЖИВИЙ pivot):** H4 — не UX-питання сприйняття (§19.3), а **поведінковий pivot-поріг**, тепер міряний ЖИВИМ publish-rate реальних користувачів (`KILL_CRITERIA_PREREG_v2.md` §6 — KILL-floor по ПОВЕДІНЦІ). **Якщо живий publish-rate показує, що публічно не публікують** (домінує показ лише в закритих чатах) — публічність-за-замовчуванням (S9a) **перевертається**, попри курс-лок §0.1 A.1; це pivot-розвилка «публічний UGC без гео-гранулярності» (`KILL_CRITERIA_PREREG_v2.md` §6/§8), не UX-твік і не pre-build гейт.

---

## 7. ФРИКЦІЙНІ ВИМОГИ (нова парадигма — наратив, не однотап) [REPLACE 2026-06-21]

Важіль зсунуто з perceived-effort-через-1-тап на композиційне тертя.

- **(а) Мінімум обов'язкового** — title + body; решта опційна → AC-S1 → важіль **тертя-входу** → `MVP`.
- **(б) Зниження композиційного тертя** — голос→текст у body, фото-старт запису, title-prefill (дата+об'єкт/сорт+1-й рядок, редагований) → AC-S1 → важіль **композиційне-тертя** → `MVP`.
- **(в) Дискретність** — МЕЖА НАРАТИВНОГО ЕПІЗОДУ (одна історія = один запис); БЕЗ milestone-чипів і БЕЗ сезонних prompt-моментів (обидва прибрано 2026-06-21; сезонні prompts — post-MVP кандидат) → важіль **дискретність** → `MVP`.
- **(г) «Білий лист»** — СВІДОМО не мітигується у v0 (рішення оператора); kill-інтерпретація — §8.
- **(ґ) Розумні дефолти** (дата=сьогодні; об'єкт=контекст), backdating окремою кнопкою (AC-S2), момент радості на локальному save-екрані (AC-S5), селектор видимості локації (AC-S9b), офлайн-захоплення (AC-S10) — PRESERVE з наявних формулювань (`design-brief` §4 ред.2026-06-21; `Бізнес-ідея_v2.txt` §0.1 C/D).
- **(д) Крос-юзерне тертя [ADD 2026-06-21]** — @-mention через tap-select typeahead (власні+чужі-публічні+сорти+@автори), не друк; claim/confirm — one-tap; інвайт — мінімум кроків → важіль **тертя-входу/згоди** → `MVP`. УВАГА: приватнісні інваріанти (AC-INV1–4) — ТВЕРДІ safety-AC, НЕ предмет фрикційного послаблення; швидкість не купується ціною експозиції.

---

## 8. ЗАКРИТТЯ — відкриті/провізорні + повтор гейта + чого PRD не покриває

**Провізорні (рішення відкладене до C2/PoC):** ~~фото primary vs опційне (C2 #1)~~ *[RETIRED 2026-06-21: primary = body]*; каталог-стандартизація / роль «Unknown» (C2 #4, AC-S7); двошаровий фід-по-сорту-в-зоні (C2 #5); пороги матчингу (PoC, `MATCHING §11`); зрозумілість селектора локації й офлайн-синк у полі (§19.3); ~~milestone-чипи як ядро (C2)~~ *[RETIRED 2026-06-21: milestone-чекбокс прибрано — див. §7(в) межа наративного епізоду]*. consistency-бейджі / QR-табличка / Year-in-Garden — `фаза-1`, не v0 (`design-brief` §4/§6).

**ПРЕ-РЕЄСТРАЦІЯ kill-інтерпретації «білого листа» (ADD 2026-06-21):** Якщо живий H1 (p_journal) валить І сигнатура провалу = «контейнер створено, записів 0-1, відтік» (порожні об'єкти/журнали — Permies-підпис), це специфічно імплікує проблему білого листа, НЕ відсутність бажання журналити. Перед висновком «p_journal хибна» — спершу ввімкнути сезонні prompt-моменти (мітигація, відкладена у v0) і пере-зміряти. Інакше ризик false-negative kill на failure-mode з дешевим непротестованим фіксом. (Узгоджено з `KILL_CRITERIA_PREREG_v2.md` §3 H1 — жива пост-лонч перевірка; інструмент не міняється.)

**Повтор рамки (несуче):** **білд v0 авторизовано (рішення оператора 2026-06-15).** H1 (p_journal), H4 (прапор Пуб, kill §6), H6 (kill §7-B) — НЕ pre-build гейти, а **ЖИВІ ПОСТ-ЛОНЧ перевірки + pivot-тригери** (деталі — банер угорі; `DRIVE2_CANON_v1.md` §4). Kill-критерії не скасовано — лишаються повноцінною фальсифікацією, лише міряються живим. Frozen `KILL_CRITERIA_PREREG_v2.md` §0/§8 не редаговано (супрерсія зафіксована ззовні рішенням оператора). Якщо живий H4 валить — S9a перевертається (§6 FLAG 1).

**Чого цей PRD НЕ покриває (за дизайном):** не визначає UI/візуал (це `Бізнес-ідея_v2.txt` §12 + дизайн-фаза); не задає метрики активації/ретеншну продукту (вони — у пілоті/`VIRALITY_RESEARCH_FINAL.md` §2 калібратори, не тут); не вирішує мотивацію показ-vs-навчання (`design-brief` §7 #6 — відкрито); не покриває bee/тварин/вино-домен (post-wave); не змінює FREEZE-LIST (скоуп білда v0 авторизовано банерами угорі — рішення оператора 2026-06-15 + розширення 2026-06-21; цей PRD не додає фіч поза S1–S20 — S14–S20 = крос-юзерний шар, додано 2026-06-21).

**[ADD 2026-06-23 — АРХІТЕКТУРНІ РІШЕННЯ D2/D3/D6/D7/D8 БЕЗ ОКРЕМИХ S-АЙТЕМІВ]:** PRD ВИЗНАЄ їх прийнятими (рішення оператора), але НЕ дублює їхню специфікацію — вони детально описані в `OverGarden_PAGE_ARCHITECTURE_v1.md` і живуть у `B5_SEO_CONTENT_ARCHITECTURE_v2.md`. **D2** — ОДИН агрегаційний двигун із типованими фасетами (сорт/проблема/регіон/курована-тема/крос) + тег-як-резолюція (не page-spawning) + курування варіант-A з асистованою промоцією популярних тегів + внутрішня адмін/курація-поверхня. **D3** — класифікація поверхонь share-test'ом (маршрут vs модал/аркуш) + редактор запису як повноекранний аркуш без власного URL + список чернеток. **D6** — профіль і повний граф-родовід публічно-видимі, але `noindex`, з індексованим провенанс-підсумком секцією на сторінці-сорті / екрані-об'єкта. **D7** — крос-сторінки `noindex` до порогу щільності, demand-gated. **D8** — homepage окремо від landing; індексовані ринкові landings (1 UA + 1 BG) з БІНДЯЧОЮ умовою keyword-диференціації від homepage + noindex-кампанійні landings. `[PAGE_ARCH_DECISIONS · D2/D3/D6/D7/D8 · рішення-оператора-2026-06-23]`.

---

## ДЕКЛАРОВАНІ ПРОГАЛИНИ

1. **«≥~80% один тап» [RETIRED 2026-06-21]** — таргет знято разом із FLAG 3 (однотап-логування більше не існує; body обов'язковий). *(Первісно: операторський таргет, не корпусне число → `[потрібен джерельний інпут для калібрування]`.)*
2. **Конфлікт-резолюція офлайн-версій** — вимога названа (`Бізнес-ідея_v2.txt` §0.1 D «конфлікти версій»), але механізм не специфіковано у джерелах → `[потрібен джерельний інпут]` (інженерне рішення + §19.3 польовий тест).
3. **Точні пороги матчингу/boosting** — навмисно не зафіксовані до PoC (`MATCHING §11`); не вигадую.
4. **Сутність журнального запису як даних** (поля події/типу) — `DB_SEED` описує КАТАЛОГ (Taxon/CatalogItem/Name), не схему журнального запису продукту; модель запису продукту → **РЕЗОЛВ (2026-06-15):** `ENTRY_DATA_AND_RANKABILITY_SPEC_v0.md` (окрема data-spec продукту, виведена з `DRIVE2_CANON_v1.md §3`; узгоджена з `DB_SEED §7.7`). *(ENTRY_DATA ревізовано 2026-06-21 під наративну модель — title+body, без типу події.)*
5. **Метрики успіху v0** (activation/retention пороги) — поза цим PRD; гейт-пороги — у FREEZE-list (`KILL_CRITERIA_PREREG_v2.md`), не дублюю.

---

**Підтвердження дисципліни:** FREEZE-LIST (`KILL_CRITERIA_PREREG_v2.md`, `C2_RECRUITING_AND_SCREENER_v4.md`, `C2_INTERVIEW_GUIDE_v2.md`, `ЦА_CANON_v1.md`) — лише цитовано як гейт/скоуп, НЕ змінено. Нових фіч/чисел/джерел не вигадано: кожна IN/OUT-фіча несе провенанс файл+секція й наявний MVP/post-MVP/C2-тег джерела. Це НОВИЙ файл; джерельні файли не змінювані. Скоуп v0 авторизовано рішеннями оператора 2026-06-15 + 2026-06-21 (банери угорі); валідація — жива пост-лонч + pivot, не C2-build-гейт.

*Кінець `OverGarden_MVP_PRD_v0.md`. Джерела: `MVP_LOGGING_DESIGN-BRIEF.md`, `MATCHING-ENGINE_STACK_SPEC.md`, `DB_SEED_AND_DATA-MODEL_SPEC_v1_2.md`, `Бізнес-ідея_v2.txt` §0.1/§11. Гейт: `KILL_CRITERIA_PREREG_v2.md`. Скоуп ЦА: `ЦА_CANON_v1.md` §5.*
