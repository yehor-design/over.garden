# Runtime Scaffold — Status & Verification

Current status: the 2026-06-26 walking skeleton is implemented and locally verified. SDD Slice 1 / Issue 1 adds the first real product path: authenticated space -> plant object -> journal entry -> object readback. SDD Slice 1 / Issue 2 adds one-photo entry upload -> quarantine -> stripped derivative processing -> original deletion -> derivative-only readback. SDD Slice 1 / Issue 3 adds offline first-entry capture with photo intent -> local queue -> manual retry -> canonical server create -> authenticated readback without duplicate entries. SDD Slice 1 / Issue 4 adds explicit entry publication -> first-publication disclosure logging -> SSR public readback -> `noindex` metadata -> derivative-only media -> public-safe search document conversion. SDD Slice 1 / Issue 5 adds recoverable entry archive/public-gone state -> authenticated archive UI -> public `410 Gone` tombstone -> search-document exclusion guard. SDD Slice 1 / Issue 6 adds object revisit -> second dated entry on the same object -> first-party H1-safe event rows with privacy payload tests. SDD Slice 2 / OVE-13 adds minimal seeded catalog tables -> signed-in catalog typeahead API -> first-entry selected/unknown catalog state -> object readback/offline payload/event enum tests. SDD Slice 2 / OVE-14 adds explicit missing-name capture -> provisional user-added catalog item -> curation queue job contract -> first-entry user_added state without treating raw text as canonical. SDD Slice 2 / OVE-15 adds a Meilisearch-derived catalog typeahead index contract -> Cyrillic-tolerant catalog search path -> Postgres fallback -> matching-worker reindex job path without leaking provisional/user-owned rows. SDD Slice 2 / OVE-16 adds revisit-time catalog resolution for Unknown/user-added objects -> selected catalog identity -> unchanged journal history -> public SSR revalidation for already-public entries. SDD Slice 2 / OVE-17 adds an internal catalog curation scaffold -> pending provisional list -> confirm/merge/reject decisions -> affected object identity updates -> typeahead reindex job enqueue. SDD Slice 3 / OVE-18 adds the first public variety aggregation surface: safe global catalog slug -> SSR `/variety/[slug]` -> active public entry aggregation -> derivative-only media -> source entry links, while staying `noindex`. SDD Slice 3 / OVE-19 adds conservative thinness promotion: `/variety/[slug]` remains `noindex` until safe public proof passes, promoted pages enter `/sitemap.xml`, JSON-LD is emitted only for promoted pages, and archive/public-gone removal de-promotes through the same server read model. SDD Slice 3 / OVE-21 adds a narrow founder seed proof path: internal curator -> one hand-authored proof block per seeded/confirmed catalog item -> draft/published state -> public variety render beside UGC without becoming a CMS or bypassing thinness promotion. SDD Slice 3 / OVE-22 adds the first public visitor activation loop: public variety CTA -> safe catalog slug carryover -> authenticated `/garden` first-entry composer with server-validated preselection -> canonical create/readback path -> enum-only activation attribution. SDD Slice 4 / OVE-23 turns the pilot workspace into a continuation surface: returning gardeners see existing plant objects first, add a follow-up entry with optional photo/offline retry through the canonical sync endpoint, and read back one derivative-only authenticated entry without creating a duplicate object. SDD Slice 4 / OVE-24 turns the public homepage into a pilot acquisition path: homepage or public-variety intent survives auth, first-entry creation uses the canonical create/readback path, and activation attribution distinguishes homepage/public-variety/direct starts with enum-only properties. SDD Slice 4 / OVE-25 adds a launch privacy gate: versioned pilot disclosure constants -> non-destructive authenticated erasure request intake -> durable minimal request row -> authenticated operator readback without journal text, media keys, raw request metadata, or precise location. SDD Slice 4 / OVE-26 adds pilot health readout: authenticated operator -> H1/H4/H6 aggregate counts/rates -> public-variety indexability trajectory -> explicit provisional labels without raw private data. See `docs/WALKING_SKELETON.md` and `docs/SDD_VERTICAL_SLICE_ROADMAP.md` for verification commands and slice rules.

OVE-27 adds an operator production-smoke surface and live smoke contract: `/garden/pilot-smoke` checks deployed env readiness without exposing secrets, `docs/PRODUCTION_PILOT_SMOKE.md` defines the browser smoke and evidence rules, and `docs/INFRASTRUCTURE_REGISTRY.md` records the current Vercel project/deployment plus public-access blockers. OVE-28 closed the temporary operator fail-open gate with `CATALOG_CURATOR_USER_IDS`; OVE-109 supersedes that legacy gate with durable `admin_user_roles` capabilities.

OVE-108 adds the admin role foundation for SDD Slice 14. `/admin` is the canonical internal admin entry, gated by durable Better Auth user roles in `admin_user_roles` (`owner`, `admin`, `moderator`, `viewer`) rather than a shared admin account or email/provider inference. Owner bootstrap runs through `pnpm admin:bootstrap-owner` with redacted output only; `/admin` renders links/status without journal text, user emails, cookies, tokens, IP/user-agent data, media keys, precise coordinates, or env values. OVE-109 moves the existing operator surfaces behind the same role/capability model: viewer-safe readouts use `operator:read`, catalog/interview/review mutations use `operator:mutate`, and maintainer-approved irreversible erasure requires `erasure:execute` for owner/admin only.

OVE-110 adds owner-controlled role management at `/admin/users`. Owners can grant or revoke `admin`, `moderator`, and `viewer` roles by exact Better Auth user id, while non-owner admin roles are denied before assignments or audit rows are read. Role changes write durable `admin_role_audit_log` rows with actor/target ids, bounded action/role/reason enums, timestamps, and a one-way session hash only; emails, cookies, raw session ids, provider tokens, IP/user-agent fields, private journal/media content, precise coordinates, and env values stay out of the table and UI. Last-owner removal or downgrade is blocked server-side.

OVE-111 adds Google OAuth sign-in without changing OverGarden identity boundaries. Better Auth enables Google only when `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` are configured, stores OAuth token material encrypted, and disables implicit email-based account linking so an unverified email/password row cannot capture a Google identity. Existing gardeners explicitly link Google from the signed-in `/garden` workspace; after linking, Google sign-in returns to the same Better Auth user id, preserving garden data, closed-pilot invite grants, and durable `admin_user_roles` access. Normal Google-created users still receive no admin role or closed-pilot write eligibility by provider, email, or domain alone. Production smoke on 2026-07-02 proved the canonical Google OAuth start uses `https://over.garden/api/auth/callback/google` and is not rejected by Google for redirect/origin mismatch.

OVE-29 hardens owner-consistent media attachment: quarantine uploads cannot pre-bind an entry id, processed media can attach only when the target entry belongs to the same user, and public journal/variety read models require media owner equality before rendering derivative photos.

OVE-30 hardens production auth startup: Better Auth uses a local fallback only outside production-like runtimes, deployed production/preview fails closed when `BETTER_AUTH_SECRET` is missing or placeholder-like, and pilot-smoke flags local-fallback auth config as a blocker without exposing secret values.

OVE-50 adds the mainline closeout guard for agent execution quality: `docs/MAINLINE_CLOSEOUT.md` defines the required Linear closeout proof, `docs/mainline-closeout-ledger.json` records required current-main proofs for OVE-29 and OVE-30, `pnpm mainline:closeout:check` verifies those commits are contained in the checked-out `main` history, and CI runs the guard before the broader web app checks. This supersedes stale roadmap/status pointers that could send the next agent back to historical Execution Batch 1 or Slice 4 work.

OVE-51 moves the selected public pilot origin from the temporary Vercel production alias to canonical `https://over.garden`. The Vercel project now has `over.garden` and `www.over.garden` attached, Cloudflare DNS points both app hosts to Vercel through DNS-only A records, Vercel production `PUBLIC_SITE_URL` and `BETTER_AUTH_URL` are set to `https://over.garden`, and the pilot-smoke readiness guard fails Vercel production if either origin regresses to the legacy `.vercel.app` alias. During live smoke, two hidden production gaps were found and fixed before closeout: `PILOT_INVITE_SIGNING_SECRET` was missing from production env, and the production DB had not yet applied `pilot_invite_grants`; the existing non-destructive app bootstrap added the missing table. Final canonical browser smoke on deployment `dpl_AkMJozhSmood7NdvSkqvfUQDySKm` proved `/`, `/health`, `/privacy`, invite claim, auth, first entry, photo derivative readback on `media.over.garden`, same-object follow-up, publish to SSR `/journal/[slug]`, `/variety/[slug]` public-variety activation, and archive-to-410 on `https://over.garden`.

OVE-52 adds segment-aware pilot decision quality before widening invites. Signed pilot invites can now carry one bounded pilot segment (`unknown_segment` by default); first authenticated write eligibility materializes that enum into `pilot_invite_grants.segment` without storing invite links, contact details, referrers, raw URLs, IP/user-agent, or free-form segment text. `/garden/pilot-learning/decision` now shows first-entry saves, same-object follow-ups, returning gardeners, and follow-up rate by segment, with explicit unknown/low-sample gaps. The provisional continue / iterate / stop evaluator can no longer claim a broad pilot pass from pooled cohort rates when the H1 signal is unknown, single-segment, only power-core, or land/practical without micro/balcony proof. Structured interview aggregates also include bounded segment counts while continuing to exclude raw transcripts, journal text, media keys, contact fields, and precise location.

OVE-54 adds a founder-only pilot rehearsal lane for periods when real external invites are unavailable. Signed invite tokens and durable grants now distinguish `closed_pilot` from `founder_rehearsal`; founder rehearsal users can exercise the same `/join` -> auth -> `/garden` write path, but pilot-health and cohort-decision aggregates filter H1/H6/value-pulse/segment/interview evidence to real `closed_pilot` grants. `/garden/pilot-health` and `/garden/pilot-learning/decision` show founder rehearsal counts separately and state that rehearsal is internal readiness evidence only, not OVE-53 field evidence. Catalog curation pilot-origin signals also require `closed_pilot` grants, so internal test provisional names do not masquerade as real pilot catalog demand.

OVE-55 adds the catalog source readiness gate for `SDD Slice 9 - Catalog Source Ingestion And Canonical Seed`. `docs/product-research/CATALOG_SOURCE_READINESS_MANIFEST.json` records the live go/no-go manifest for source paths, `docs/product-research/CATALOG_SOURCE_READINESS.md` gives the operator-readable summary, and `pnpm catalog:sources:verify` validates the manifest plus live endpoint checks without bulk importing data or scraping vendor/marketplace paths. The approved first ingestion sources are UA State Register, CoL/ChecklistBank, WFO, GBIF Backbone, EPPO, Wikidata, GRIN, and VBO; OVE-100 later adds a separate EU OJ/EUR-Lex approved source path. Conditional/internal-only sources must not feed canonical projections until their blockers are closed. External occurrence/distribution coordinates remain raw/source-only and are not OverGarden user/product location data.

OVE-79 extends the OVE-55 gate into the full catalog import readiness contract for `SDD Slice 12 - Full Catalog Import Rollout`. `docs/product-research/CATALOG_SOURCE_READINESS_MANIFEST.json` now includes `fullImportReadiness` with one verdict per source family, explicit raw-quarantine and product-projection booleans, product projection mode, source-only fields, unblocking evidence, next Linear dependency, and import wave membership. `pnpm catalog:sources:verify` validates both the live endpoints and the machine-readable wave contract. After OVE-87, the current full-import wave counts are raw=13, product=9, review=6, legal=9, parser=4, rejected=1. This is still a gate, not a bulk import: OVE-80 dry-run, OVE-81/82/83/86 source-family projections, OVE-84 blocking and OVE-87 PGR source-use gates, OVE-85/88 curated imports, OVE-89 entity-resolution QA, and OVE-90 production proof must link back to this readiness contract before claiming full catalog availability.

OVE-80 adds the full-import dry-run harness for the catalog rollout. `pnpm catalog:sources:dry-run -- --environment local --confirm-environment local` emits a redacted `ove80.catalogFullImportDryRun.v1` report without reading env files, connecting to the database, calling external source APIs, or mutating local/staging/preview/production data. The report covers the existing proof importers, source row and raw/quarantine capture counts, product projection counts, review/reject/block counts, attribution counts, OVE-79 source verdicts, existing projection-guard checks, duplicate-risk clusters for OVE-89, and a fail-closed leak check. Non-local preflight requires `--preflight-only`. Later source-family import issues must attach this report or a target-specific successor before bulk mutation or new product projection.

OVE-81 expands the UA State Register importer from the original proof row to the approved full official-variety wave. `pnpm catalog:sources:import-ua-register-variety` now verifies the approved file proof, imports 15,177 valid official variety concepts idempotently, projects 61,105 safe official-name/transliteration/taxon-qualified aliases to catalog/typeahead, records zero parser rejects, leaves repeated official denomination clusters for OVE-89 entity-resolution review, and emits only redacted count/sample evidence. The real `/garden` catalog UX smoke now includes representative UA variety cases beyond `Ботсадівський` while continuing to reject raw payloads, source-only register fields, source row identifiers, exact location markers, media internals, and user-owned data from output.

OVE-83 expands the reviewed vernacular alias layer on top of the OVE-82 species-backbone wave. `pnpm catalog:sources:import-species-backbone` now projects 30 accepted species aliases across tomato, cucumber, sunflower, and basil, including reviewed Ukrainian, Bulgarian, English, and EPPO/Wikidata-backed local names such as `помідори`, `домати`, `огірок звичайний`, `common sunflower`, `сонях`, `sweet basil`, `базилік духмяний`, and `обикновен босилек`. Ten non-accepted alias candidates remain in `catalog_alias_projections` only: `review_needed`, `rejected`, and `generated` rows have no `catalog_item_name_id`, do not reach typeahead/Meilisearch/product UI, and are covered by import proof plus the real `/garden` smoke. The OVE-80 dry-run harness now exposes an explicit `vernacular-alias-expansion` target for OVE-83 with 31 alias candidates, 21 product-visible vernacular aliases, 10 blocked aliases, no new raw taxonomy rows, and OVE-89 duplicate/collision review as the downstream gate.

OVE-84 closes the BG official-variety IASAS/portal bulk gate as an explicit block, not as import approval. `docs/product-research/CATALOG_SOURCE_READINESS_MANIFEST.json` includes `fullImportReadiness.bgOfficialVarietyBulkGate` with `fullRawImportAllowed = false`, `productProjectionAllowed = false`, and `boundedProofProjectionAllowed = true` for the existing OVE-61 `Садово 1` proof only. Live checks verify the IASAS 2026 OSL PDF byte path and EU Plant Variety Portal legal-value caveat page, while documenting the concrete blockers: no approved structured IASAS export path, IASAS reuse basis not cleared, EU portal information-only/no-legal-value caveat, no full parser QA, and no row-level attribution/caveat mapping. The source projection guard reports these blockers from the manifest and rejects attempts to treat OVE-84 as a bulk product-projection gate before OVE-85.

OVE-87 closes the PGR/genebank bulk gate as a partial approval, not broad genebank import. `docs/product-research/CATALOG_SOURCE_READINESS_MANIFEST.json` includes `fullImportReadiness.pgrGenebankBulkGate`: GRIN/NPGS is allowed for raw/source quarantine and curator-only candidate projection with required provenance, while Genesys and EURISCO remain internal-validation-only/legal-blocked until written permission or a legal memo clears raw storage and product projection. `grin-global.productProjectionPolicy` now requires GRIN source URL provenance, `grin_genebank_candidate`, and `GRIN:NPGS:OVE62:` or `GRIN:NPGS:OVE88:` source record keys. The `pgr-genebank-bulk-gate` dry-run target reports this boundary without capturing raw rows or projecting product concepts.

OVE-88 expands the approved GRIN/NPGS path from the OVE-62 two-row proof into a bounded bulk quarantine proof. `pnpm catalog:sources:import-genebank-long-tail` now imports 12 curated GRIN/NPGS proof rows into source snapshots/records, keeps accession identifiers, germplasm distribution caveats, legal blockers, and raw/source-only fields out of product evidence, promotes only three approved plant-variety candidates (`Red Cherry tomato`, `Bulgarian Carrot pepper`, `Odessa Market tomato`) through per-row projection guards, and proves held, review-needed, rejected, and blocked candidates stay out of typeahead. `/garden/catalog/curation` now shows aggregate-safe source-candidate counts and status filters for quarantined, held, review-needed, blocked, rejected, and promoted rows without selecting raw payloads or source-only fields. The real `/garden` smoke includes the promoted OVE-88 candidates plus absence checks for non-promoted genebank proof names.

OVE-89 adds the entity-resolution QA gate before production full-catalog proof. `pnpm catalog:sources:entity-resolution-qa` reads the current source-backed catalog state and emits a redacted `ove89.catalogEntityResolutionQa.v1` report for likely duplicates, alias collisions, source disagreements, blocked projection groups, and manual-review rows. `/garden/catalog/curation` renders the same safe report for role-gated operators with `operator:mutate`. The report and UI expose catalog identity, public slug, source family, status, and aggregate counts only; raw payloads, source-only fields, source record keys, journal text, owner data, media internals, and precise location remain excluded. OVE-90 must attach this QA evidence and cannot treat unresolved duplicate/conflict clusters as trusted full-catalog availability.

OVE-100 approves the EU Official Journal / EUR-Lex Common Catalogue source path without reclassifying IASAS or EU Plant Variety Portal-only rows. The new `eu-oj-eur-lex-common-catalogue` manifest entry is `USE` with `canonical_product_projection`, but the source projection guard allows rows only when `sourceUrl` is a stable EUR-Lex or `data.europa.eu` ELI link, `productSource = eu_oj_eur_lex_common_catalogue`, source version/key/id provenance is present, attribution text is recorded, and the legal-value caveat is preserved. IASAS PDFs, portal-only rows, CPVO/UPOV rows, and missing-provenance rows remain source-only.

OVE-101 adds the explicit EUR-Lex / Official Journal inventory dry-run target. `pnpm catalog:sources:dry-run -- --environment local --confirm-environment local --target eu-official-journal-common-catalogue` discovers the latest DG SANTE Common Catalogue Supplement A and Supplement H EUR-Lex links, fetches the EUR-Lex source pages, preferred XML notices, and Publications Office CELEX RDF metadata, records ELI/CELEX/Cellar URLs plus checksums for fetched artifacts, and keeps the target inventory-only with `productConceptsWouldProject = 0`, `aliasesWouldProject = 0`, and `checkedProjectionRequests = 0`. PDF remains a human/legal fallback, not the preferred parser input; unavailable or ambiguous machine artifacts are reported as `review_needed`.

OVE-102 extends the EUR-Lex / Official Journal target from inventory into operator parser QA. The dry-run discovers the Publications Office Formex XML ZIP from the EUR-Lex XML notice, or derives the deterministic Publications Office ZIP URL from CELEX when EUR-Lex XML is WAF-challenged, parses OJ-derived agricultural Supplement A and vegetable Supplement H variety rows, preserves denomination/species/notifier/country/action/OJ/source/checksum/parser confidence fields in bounded evidence, and reports counts by supplement, species/crop, country/notifier, and confidence bucket. Accepted, review-needed, and rejected rows stay pre-import evidence only: `productConceptsWouldProject = 0`, `aliasesWouldProject = 0`, and IASAS-only or portal-only rows still cannot enter this parser path.

OVE-103 imports accepted Official Journal / EUR-Lex Common Catalogue rows as `eu_oj_eur_lex_common_catalogue` plant-variety concepts with source attribution, stable ELI/OJ URL provenance, parser/extraction versions, OverGarden normalization caveat, legal-value caveat, idempotent source snapshots/records/links, and catalog typeahead reindex intent. Review-needed and rejected parser rows remain source-only; raw payloads, source record keys, parser diagnostics, checksums, notifier/action fields, legal caveats, and source-only metadata stay out of Meilisearch/typeahead/product identity.

OVE-104 adds the local/main `/garden` smoke gate for the OVE-103 path before OVE-85-90 resume. `pnpm smoke:garden-eu-oj-common-catalogue` signs in through the real app, opens `/garden`, searches authenticated typeahead, saves an EU OJ-backed selected variety through `/api/garden/entries`, reads back `/garden/objects/[objectId]`, and verifies `Source: Official Journal of the European Union / EUR-Lex. Normalized by OverGarden.` plus the legal-value caveat. The smoke uses a Bulgaria-relevant accepted OJ row when available and reports explicitly when the current approved artifact has none. This is not production proof; OVE-90 remains the production rollout proof issue.

OVE-105 adds the guarded production landing step for the OVE-103 Official Journal / EUR-Lex Common Catalogue importer. `pnpm catalog:sources:import-eu-oj-common-catalogue -- --environment production --confirm-environment production --allow-non-local-mutation --base-url https://over.garden` now refuses non-local mutation without explicit environment confirmation, HTTPS app URL, and a non-local database target. The command emits `ove105.euOjProductionLanding.v1` redacted evidence covering accepted-row product visibility, idempotent rerun counts, source provenance/attribution/caveat presence, and blocked-row absence without exposing source-row keys, raw payloads, checksums, env values, secrets, user data, or precise coordinates. Production landing passed on 2026-07-02 at commit `a74cc976a286863fe9f0a5b3d221ee972a5c1608` with CI run `https://github.com/yehor-design/over.garden/actions/runs/28583649110`: 2 snapshots, 1,075 source records, 721 product-visible accepted rows, 353 quarantined rows, 1 rejected row, stable idempotent rerun identity, sample `Artic` typeahead/provenance proof, blocked-row status `quarantined`, and leak check `passed`. OVE-106 must still prove the production `/garden` UX/search path before OVE-90 resumes.

OVE-85 adds the BG official-varieties full-import subset proof without reopening the blocked IASAS/PVP path. The OVE-80 dry-run now has `--target bg-official-varieties`, which uses only `eu-oj-eur-lex-common-catalogue`, filters parser QA to Bulgaria-notified rows, and reports BG accepted, review-needed, rejected, blocked, attribution, and OVE-89 duplicate-risk counts. `pnpm smoke:garden-bg-official-varieties` proves a Bulgaria-relevant OJ-backed `plant_variety` can be selected in `/garden` beyond `Садово 1`, keeps the OVE-61 `Садово 1` proof selectable, renders EU OJ attribution/legal-value caveat on readback, and confirms review-needed/rejected OJ rows plus IASAS-only rows have no product selection link. This remains local/main proof; OVE-90 is still the production rollout gate.

OVE-90 adds the final full-catalog production rollout proof command. `pnpm catalog:sources:production-rollout-proof -- --environment production --confirm-environment production --allow-non-local-mutation --base-url https://over.garden` reads already-landed production source-family rows without running source ingestion, runs the full real `/garden` catalog UX smoke plus the BG official-varieties smoke, reads the OVE-89 entity-resolution QA report, rebuilds the derived Meilisearch `catalog_typeahead` index from safe catalog rows, and checks representative UA variety, species, breed, GRIN/NPGS, and EU OJ/BG rows through both direct Postgres fallback and Meilisearch. The command emits only `ove90.fullCatalogProductionRolloutProof.v1` redacted evidence and fails closed on unresolved likely-duplicate/source-disagreement clusters, duplicate same-concept suggestions, blocked-row leaks, missing Meilisearch proof, local/non-local database mixups, source-ingestion assumptions, or forbidden evidence markers.

OVE-107 adds the missing production landing gate for the approved non-EU rows that OVE-90 samples after OVE-106. It uses the guarded `pnpm catalog:sources:seed-rollout-proof -- --environment production --confirm-environment production --allow-non-local-mutation --base-url https://over.garden` path to land the OVE-81 UA State Register wave, OVE-86 VBO animal-breed subset, and OVE-88 promoted GRIN/NPGS rows before OVE-90 is rerun. The UA State Register importer now performs chunked upserts for source records, catalog items, aliases, and source links so the approved full wave can land in production without the previous row-by-row transaction stall. OVE-107 does not close OVE-90; it only removes the remaining production data prerequisite and records redacted production evidence.

OVE-71 establishes the Apple Container-first runtime doctrine for local containerized development. OVE-72 adds the repo-local Apple Container service path: `infra/container-up`, `infra/container-status`, and `infra/container-down` manage local Postgres, Meilisearch, and MinIO on the same ports and env contracts as the prior Docker Compose scaffold, with named Apple Container volumes for persistence. Current runtime authority is `docs/CONTAINER_RUNTIME_POLICY.md`: supported local Mac work should use Apple Container first; Docker remains only as a documented fallback for unsupported hosts, temporary migration gaps, Ubuntu CI, and the current Linux production worker/search droplet.

OVE-73 proves the fresh-checkout web bootstrap against the Apple Container local service trio with Docker Desktop stopped. A supported-Mac agent can start or reuse `infra/container-up`, copy `apps/web/.env.example` to `apps/web/.env.local`, run the normal web setup/check path, and pass `pnpm local:bootstrap`, `pnpm db:types:check`, lint, typecheck, tests, and build without relying on Docker Desktop. Older Docker Compose walking-skeleton notes are historical proof/fallback context, not the default local runtime path.

OVE-74 proves the matching-tier local image and worker/search smoke against Apple Container. `container build -t overgarden/matching:local .` succeeds for `services/matching/Dockerfile`, the FastAPI health process starts from the Apple Container-built image with ICU/PyICU loaded, native worker recovery tests pass, and the Meilisearch Cyrillic/catalog typeahead proof runs against Apple Container Meili. The OVE-74 run also fixed the catalog typeahead document id to use a Meilisearch-safe ASCII key for Cyrillic aliases; the searchable alias fields remain Cyrillic-safe and public/curated only. Docker remains fallback-only for unsupported local hosts or verified feature gaps, and remains production-Linux tooling until OVE-76.

OVE-75 documents the CI runtime boundary. GitHub Actions remains on `ubuntu-latest`, and its Postgres service container plus MinIO `docker run` remain Docker-backed because Apple Container is not an Ubuntu service-container runtime. CI coverage is unchanged; this is a `ci-required` exception and does not reintroduce Docker Desktop as a supported-Mac local prerequisite. Any future Apple Container CI replacement must first document runner support, cost/availability/concurrency, service-contract proof commands, and fallback behavior in `docs/CONTAINER_RUNTIME_POLICY.md`.

OVE-76 documents the production Linux runtime boundary. Apple Container remains the preferred supported-Mac local runtime, but it is not the DigitalOcean Linux droplet process manager. Docker Compose stays on `overgarden-worker-prod-fra1` for `matching-worker`, `matching-api`, `meilisearch`, and `caddy` because OVE-39 live-proved restart policy, health endpoints, and journal publish/index plus archive/unindex recovery there. Any non-Docker production replacement is a separate migration and must live-prove equivalent process restart/reboot recovery, worker/search health, public-safe Meilisearch document shape, and redacted evidence handling before these production instructions can change.

OVE-77 completes the local Docker fallback cleanup. On 2026-06-30, `command -v container` resolved `/usr/local/bin/container`, `container system status` reported `running` for `container-apiserver` 1.0.0, `infra/container-up` reused ready Apple Container Postgres/Meilisearch/MinIO services, and the Docker Desktop/com.docker process check returned no Docker process beyond the check command itself. The closeout proof passed `pnpm local:bootstrap`, `pnpm db:types:check`, `pnpm test`, and `uv run --frozen pytest` in `services/matching`. Result: on supported Apple Silicon/macOS 26 machines, Docker Desktop can be uninstalled locally. Docker remains only for unsupported hosts, verified Apple Container feature gaps, GitHub Actions Ubuntu CI, and the production Linux worker/search droplet.

OVE-95 aligns the local and CI Postgres major-version contract with production. `postgres:18-alpine` is the default for GitHub Actions, Apple Container local services, and Docker fallback. Apple Container local Postgres now uses the version-specific `overgarden-postgres-18-data` volume and fails fast when an existing `overgarden-postgres` container still points at the older image or volume, telling the operator to run `infra/container-up --recreate` so the old named volume is preserved instead of silently reusing Postgres 16.

Post-update host proof on 2026-06-30 confirmed this path on Apple Silicon macOS 26.5.2 build 25F84. `container system start` registered and launched `container-apiserver` 1.0.0 after the OS update, `infra/container-up` and `infra/container-status` reported Postgres, Meilisearch, and MinIO ready through Apple Container, `docker` was not installed on PATH, and the only remaining Docker-named process was the privileged helper `/Library/PrivilegedHelperTools/com.docker.vmnetd`, not Docker Desktop or a Docker daemon. The proof passed `pnpm local:bootstrap`, `pnpm db:types:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm mainline:closeout:check`, a local-secret `pnpm build`, and `uv run --frozen pytest` in `services/matching`.

OVE-56 adds the first catalog source snapshot quarantine proof. The app schema now has `catalog_source_snapshots`, `catalog_source_records`, and `catalog_source_links` so a legal source payload can be stored with source/version/license/checksum/parser provenance while only an allowlisted projection reaches `catalog_items` and `catalog_item_names`. `pnpm catalog:sources:import-sample` imports the OVE-55 UA Register `Bergeron 1` sample idempotently, queues the derived typeahead reindex, proves safe typeahead from catalog tables, and proves a temporary rollback-only garden readback with `variety_state = selected`. Raw/source-only poison fields stay confined to `catalog_source_records` and are rejected from catalog typeahead/Meili hit mapping.

OVE-57 adds the first official UA State Register variety projection. `pnpm catalog:sources:import-ua-register-variety` downloads the exact approved `2025-07-15_registervarietis.csv` file, decodes UTF-16LE, records full-file checksum/byte length/row count, imports `RegisterVarietis:83070006` (`Ботсадівський`) idempotently, projects only the official Ukrainian display name plus official transliteration aliases to catalog/typeahead, queues the derived catalog reindex, proves Ukrainian and transliteration typeahead, proves rollback-only selected garden readback, and proves operator provenance readback from `/garden/catalog/curation` without exposing raw payload/source-only fields. Existing provisional catalog curation can merge a user-added candidate into the imported official item without moving journal entries.

OVE-58 adds the first canonical species-backbone seed through the real catalog selection path. `pnpm catalog:sources:import-species-backbone` imports `Solanum lycopersicum L.` from the OVE-55 approved CoL/WFO/GBIF/EPPO/Wikidata source set, keeps one source snapshot/record/link per source, preserves `col_id`, `wfo_id`, `gbif_taxon_key`, `eppo_code`, and `wikidata_id` in internal provenance/allowed projection, projects only the accepted scientific name, one EPPO-backed synonym, and reviewed safe aliases (`Tomato`, `помідор`, `томати`, `домат`) into `catalog_items`/`catalog_item_names`, queues typeahead reindex, proves scientific-name/synonym/common-name typeahead, and proves rollback-only selected garden readback. GBIF occurrence coordinates, EPPO distribution text, raw payloads, and non-reviewed aliases stay quarantined in `catalog_source_records`; Meili/typeahead hit mapping now rejects external source IDs as well as raw/source-only fields. The internal source provenance view also shows projected aliases from `catalog_item_names` without selecting raw payloads.

OVE-59 adds vernacular alias promotion states on top of the species-backbone seed. `catalog_alias_projections` records each imported/generated alias candidate with explicit status (`accepted`, `review_needed`, `rejected`, `generated`, `user_provisional`), language/locale/script, source slug, source method, source record key, confidence, license, attribution, and projection notes. Only `accepted` aliases are linked to `catalog_item_names` and therefore reach typeahead/Meili; `garden tomato` remains review-needed, `love apple` remains rejected, and generated `помидор` remains operator-review-only. `pnpm catalog:sources:import-species-backbone` now proves accepted local/common alias lookup to the canonical `Solanum lycopersicum L.` item, rollback-only selected garden readback, blocked non-accepted alias typeahead, and operator alias provenance without raw payload/source-only fields. Existing provisional user-added catalog confirm/merge remains separate from imported alias promotion and still does not move journal entries.

OVE-60 adds the first breed seed proof without forcing breeds into the plant variety model. `catalog_items.catalog_kind` now distinguishes `plant_variety`, `species`, and `breed`, while `plant_objects.object_kind` distinguishes `plant`, `bee_colony`, and `animal` for user readback. `pnpm catalog:sources:import-breed-seed` imports the manual official Ukrainian bee breed seed for `Карпатська бджола` idempotently, records source snapshot/record/link provenance against Law of Ukraine On Beekeeping No. 1492-III, projects safe Ukrainian/English aliases to typeahead as `catalogKind = breed`, keeps VBO and DAD-IS/EFABIS validation notes source-only/internal-only, holds disputed Latin mappings for curator review, proves rollback-only garden readback as `objectKind = bee_colony`, and rejects breed source-only fields from Meili/typeahead hits.

OVE-86 expands that breed path without opening blocked breed sources. `pnpm catalog:sources:import-breed-seed` now imports five approved breed concepts: three manual official Ukrainian bee breeds (`Карпатська бджола`, `Українська степова бджола`, `Поліська бджола`) plus the VBO-supported animal-breed subset `Ukrainian Grey (Cattle)` and `Bulgarian Rhodope (Cattle)`. The importer records per-source snapshots for the official Ukrainian bee seed and VBO, keeps DAD-IS/EFABIS rows and generated/localized VBO labels source-only or review-only, projects only accepted aliases to typeahead, and proves readback as `bee_colony` for bee breeds and `animal` for VBO vertebrate breeds. The first-entry UI, API fallback, and smoke harness now map breed object kind by source instead of treating every breed as a bee colony.

OVE-61 adds the first Bulgarian official-variety proof without opening a full BG catalog import. `pnpm catalog:sources:import-bg-official-variety` imports one reviewed EU/BG proof row for `Садово 1` idempotently, stores EU Plant Variety Portal source/version/legal-caveat attribution plus IASAS 2026 OSL PDF proof metadata, projects only the cleared BG/Latin aliases to catalog/typeahead, proves rollback-only selected garden readback as a plant object, and keeps the low-confidence IASAS PDF row quarantined with parser/legal status out of typeahead. Meilisearch/typeahead hit mapping now also rejects BG/EU parser confidence, legal caveat, source page/row reference, national-id, and IASAS PDF parser-blocker fields if they appear accidentally.

OVE-62 adds the genebank long-tail candidate proof without opening blocked PGR sources or live scraping. `pnpm catalog:sources:import-genebank-long-tail` imports a bounded GRIN/NPGS proof subset into source snapshots/records, keeps accession identifiers, germplasm distribution caveats, and Genesys/EURISCO legal blockers source-only, exposes only safe review/provenance metadata to the operator, promotes the single curator-approved `Red Cherry` candidate into catalog/typeahead, proves scientific-alias lookup and rollback-only selected garden readback as a plant object, and keeps the held landrace candidate quarantined out of typeahead. Meilisearch/typeahead hit mapping now rejects genebank accession, review, curator-decision, improvement-level, germplasm-policy, and redistribution/anti-compete fields if they appear accidentally.

OVE-63 adds the required source-attribution path for imported catalog facts. `catalog_source_snapshots` now records `license_url` and `attribution_text` alongside source name/version/source URL/license/attribution flag, and the UA Register plus species-backbone importers populate those fields for CC-BY and EPPO attribution-required sources. `/garden/catalog/curation` links safe source/license credits in operator provenance, while `/variety/[slug]` renders a public source-credits section only from a bounded read model for projected canonical facts whose sources require attribution. That public read model selects source name, version, source URL, license, license URL, attribution flag, and attribution text only; it does not select raw payloads, source-only fields, source record keys, checksums, external IDs, occurrence/distribution coordinates, journal text, owner data, or media internals. Public variety JSON-LD remains limited to safe CollectionPage/entry metadata, and Meilisearch/typeahead hit mapping rejects attribution credit fields if they appear accidentally.

OVE-64 adds the first source refresh diff proof for an already imported UA Register source. `catalog_source_refresh_events` and `catalog_source_refresh_records` now audit deterministic refresh runs with `new`, `unchanged`, `changed`, `removed_upstream`, `parser_reject`, `review_needed`, and `projection_blocked` rows. `pnpm catalog:sources:refresh-sample` seeds a previous source snapshot, refreshes a newer fixture snapshot without putting snapshot version into canonical `catalog_items.source_id`, projects a new accepted record, projects only safe alias changes for an existing record, queues curator review for canonical-name drift, retains disappeared upstream rows without deletion, blocks license/status drift from product projection, proves typeahead for accepted changes, proves rollback-only user readback keeps the same catalog item, and keeps raw/source-only fields out of operator output and typeahead.

OVE-65 hardens source-backed seed read models before adding new catalog sources. Product typeahead now dedupes source-backed suggestions by deterministic canonical concept key so repeated proof fixtures, such as OVE-56 `Bergeron 1` plus OVE-64 refresh `Bergeron 1`, return one selectable suggestion while non-source-backed rows remain item-scoped. The Postgres fallback overfetches safe rows before dedupe so duplicates do not consume the visible suggestion limit. Operator source provenance now presents a current/latest summary per source-backed concept record with an audit-link count, preserving historical `catalog_source_links` and source records for debugging without showing noisy repeated parser/snapshot rows in the curation UI.

OVE-66 adds a compact internal source-candidate review lane to `/garden/catalog/curation`. A role-gated operator with `operator:mutate` can review source-backed candidates grouped as quarantined, held, review-needed, blocked, rejected, or promoted; inspect safe source/legal/status badges and projection preview; promote approved GRIN/NPGS proof candidates through a server action; and hold or reject quarantined review rows without touching journal entries. The lane reads only snapshot metadata plus curated `allowed_projection` fields, never raw payloads, source-only fields, external coordinates, journal text, media internals, owner data, analytics, or precise location. Held/rejected/blocked rows stay out of product typeahead; promotion still passes the source projection guard and enqueues catalog typeahead reindex.

OVE-67 adds a real garden catalog UX smoke over the source-backed seed set. After OVE-81/82/83/86/88, the smoke signs in through the closed-pilot gate, visits `/garden`, searches representative official-variety, species, breed, and genebank queries, including reviewed species aliases such as `помідори`, `домати`, `огірок звичайний`, `common sunflower`, `сонях`, `sweet basil`, `базилік духмяний`, and `обикновен босилек`, plus GRIN/NPGS candidates `Red Cherry`, `Bulgarian Carrot`, and `Odessa Market`, then saves each accepted catalog suggestion through the canonical first-entry path. It also checks blocked alias inputs such as `garden tomato`, `love apple`, `помидор`, `gherkin`, `pickle`, `holy basil`, review-only VBO localized labels such as `Українська сіра`, and non-promoted genebank proof names do not appear as product alias suggestions. Authenticated readback verifies deduped suggestions, kind-correct labels (`Plant variety`, `Plant species`, `Bee breed`, `Animal breed`), expected object kind including `bee_colony` and `animal`, selected catalog identity, hidden location, and no source-only/operator metadata in evidence. `/api/garden/catalog/typeahead` now falls back to canonical Postgres search when the derived Meilisearch index returns no hits, so stale derived search cannot make seeded catalog suggestions disappear from the pilot UX.

OVE-68 adds the source-expansion product-projection guard before additional catalog sources can enter the product surface. Source-backed importers now check the OVE-55 readiness manifest before writing `catalog_items` or product-visible `catalog_item_names`: a source must be `USE` with `canonical_product_projection`, or it must match a named source-specific gate for one bounded source/version/record path. The OVE-60 official bee manual seed, the OVE-86 two-row official bee expansion, and OVE-61 BG reviewed subset remain explicit bounded exceptions, not bulk approvals; conditional/internal/rejected/unknown sources, including broader EU/Common Catalogue or IASAS rows, DAD-IS/EFABIS, EURISCO, Genesys, and vendor/marketplace paths, fail closed before product projection until a fresh gate clears that exact use.

OVE-69 adds an explicit catalog seed rollout proof instead of treating deployed code or local importer success as environment state. `pnpm catalog:sources:seed-rollout-proof -- --environment <local|staging|preview|production> --confirm-environment <same> --base-url <app-url>` runs the approved source-backed product proof set with captured importer output, emits only a redacted environment summary, and runs the real `/garden` catalog UX smoke against the named app URL. Non-local runs require `--allow-non-local-mutation`; no staging or production seed state is claimed unless that exact command is run against the selected environment and the redacted evidence is recorded. After OVE-88, the rollout proof includes representative UA State Register rows, reviewed species alias expansion, blocked alias absence, `Карпатська`, `Ukrainian Grey`, `Садово 1`, `Red Cherry`, `Bulgarian Carrot`, and `Odessa Market`; conditional/internal/vendor sources remain unseeded.

OVE-70 isolates source-import proof harness output so clean-state gates and rerun idempotency are no longer conflated. The GRIN/NPGS proof command still runs idempotently, but clean-state absence is labeled only when the promotable candidate is still quarantined and absent from product typeahead. Reruns with an already promoted candidate now report `skipped_existing_projection` plus `rerunExistingProjection`, and `--require-clean-state` fails loudly if the caller needs a clean-state proof rather than an idempotency proof.

OVE-33 adds a fresh-checkout drift guard: CI starts Postgres plus MinIO, runs `pnpm local:bootstrap`, then runs `pnpm db:types:check` so SQL migrations, Better Auth bootstrap tables, object-storage bucket assumptions, and committed Kysely generated types cannot drift silently before lint/typecheck/test/build.

OVE-34 replaces pilot-placeholder privacy/publication/erasure copy with closed-pilot reviewed copy while keeping public release blocked. OVE-94 bumps the first-publication disclosure to version `first-publication-v3` for plain-language archive wording. OVE-97 bumps erasure intake to `erasure-request-pilot-v3` and adds the maintainer-approved irreversible erasure execution path after dry-run review. Users can see the latest erasure request status, and the operator review surface can move requests through submitted -> reviewing -> handled without selecting journal text or media keys.

OVE-35 hardens the pilot journal UX language for real users. `/garden`, first-entry capture, same-object follow-up capture, object readback, catalog resolution, publish/archive disclosure copy, `/privacy`, and `/first-publication-disclosure` now describe local recovery, catalog match state, publication, archive, search-engine visibility, and photo safety in user-facing language rather than exposing terms such as local queue, sync, photo intent, lifecycle state, noindex, or stripped derivative. The underlying canonical routes, idempotent offline sync payloads, publication/archive behavior, and privacy boundaries are unchanged.

OVE-37 proved the full first-user smoke on current `main` at the historical public pilot URL `https://over-garden.vercel.app`. Against production deployment `dpl_5xY21uia8usEAdA7LoLwhYTXhUB5` (commit `a8cd3c95`, ref `main`, verified), `/`, `/health`, and `/privacy` returned `200` OverGarden HTML without Vercel SSO; auth on the pilot origin, first entry, same-object follow-up, publish to SSR `/journal/[slug]` (`200`, `noindex, nofollow`, no precise location, no quarantine/original keys), public `/variety/[slug]` with a safe-slug `public-variety` activation CTA, the activation save through `/garden`, and archive to `410 Gone` all passed. The photo derivative path (presigned quarantine upload -> server strip/re-encode -> `media.over.garden` `image/webp` derivative-only readback, original deleted) was proven through the authenticated media API plus a verified quarantine CORS preflight for the pilot origin, because the agent browser cannot drive the OS file picker. Publish enqueues `journal_entry_index` and archive enqueues `journal_entry_unindex`; the live index/unindex round-trip remains proven by the standing OVE-36 canary. OVE-51 supersedes the alias as the selected pilot origin. Redacted evidence is recorded in `docs/PRODUCTION_PILOT_SMOKE.md` and `docs/INFRASTRUCTURE_REGISTRY.md`.

OVE-39 proves production durability for the pilot journal search path without weakening any privacy boundary. DigitalOcean Managed PostgreSQL backup/PITR for `overgarden-postgres-prod-fra1` is verified as `pass` on 2026-06-29 through redacted `doctl` backup listing evidence, with the latest observed backup on 2026-06-28 17:33 UTC and a 7d PITR/retention window recorded per provider docs/defaults. The worker/process-manager story is live-verified without secrets: Docker Compose on the worker droplet, `unless-stopped` restart policy for `matching-worker`, `matching-api`, `meilisearch`, and `caddy`, matching health `ok` with ICU present, and Meilisearch health `available`. The live smoke restarted only `matching-worker`, then proved publish/index and archive/unindex through the production app path: `journal_entry_index` reached `done`, the Meilisearch document kept exactly the public-safe OVE-36 key contract with `noindex=true` and no forbidden private fields, `journal_entry_unindex` reached `done`, the document returned `404`, and the old public journal URL returned `410`. The deterministic recovery harness (`services/matching/tests/test_worker_recovery.py`) still proves stale-`processing` reclaim through `FOR UPDATE SKIP LOCKED` and `WORKER_VT_SECONDS`, idempotent at-least-once handling, and transient Meilisearch outage recovery. Redacted evidence is in `docs/INFRASTRUCTURE_REGISTRY.md` and `docs/PRODUCTION_PILOT_SMOKE.md`. No restore-over-production, fork/restore, bulk delete, schema drop, production DB restart, all-container restart, or history rewrite was performed.

OVE-98 makes the public journal Meilisearch schema contract explicit. The runtime writer remains `services/matching/app/search.py:journal_entry_search_document_from_row`; the machine-readable allowed/forbidden field contract lives in `contracts/search/public-journal-entry-search-document.json`. The TypeScript search document builder is now labeled as a privacy contract fixture, and both TS and Python tests read the same contract so future field drift fails in both runtimes before it reaches Meilisearch.

OVE-96 resolves the current lineage/social graph scope conflict before implementation starts. `docs/LINEAGE_SCOPE_DECISION.md` is now the repo-local decision: lineage, provenance edges, handles, follows, claim inbox, non-user invitations, ask-the-lineage flows, graph UI, cross-user typeahead, and social graph jobs are post-MVP for current execution. Future agents must not implement historical S14-S20 lineage scope from product-research files or historical Slice 6 roadmap text until the decision file's real closed-pilot H1/H4 gates are met or a newer dated founder/operator decision supersedes it. Current scope remains journal capture, safe publication/archive, catalog/typeahead, privacy/erasure readiness, pilot learning, and public UGC/SEO readiness.

OVE-40 adds an integrated privacy invariant sweep so privacy boundaries are machine-checked across one realistic user journey (private first entry -> photo derivative -> publish -> public journal SSR -> variety aggregation/JSON-LD -> archive/410 -> erasure intake -> operator readouts), not only remembered in docs. A shared poison fixture (`apps/web/test/privacy/`) seeds every private/operator-only field with unique sentinel values, and a reusable scanner asserts no forbidden field name or value reaches the public journal HTML, journal/catalog Meilisearch document conversion, public variety JSON-LD, analytics event properties, the operator erasure list query, the pilot-health aggregate readout, or the pilot smoke readiness evidence. The sweep also asserts hidden-vs-region visibility (a precise string in the coarse slot resolves to no label and never renders/indexes), derivative-only media (the public renderer emits only the public derivative URL, never the internal media key), HTML escaping of user markup, and archived/public-gone exclusion from search. Static structural checks read the SQL schema and generated Kysely types and fail if any precise-location column (latitude/longitude/coordinate/geo\*/gps/precise location) is ever introduced, while asserting the coarse-region controlled vocabulary, the quarantine/derivative media boundary, and the bounded analytics jsonb object stay in place. The public journal HTML renderer was extracted to `src/app/journal/[slug]/render.ts` so the public bytes can be asserted directly; a leak deliberately injected into the renderer was confirmed to fail the sweep before revert. Machine-checked now: the seven public/search/analytics/operator outputs above plus the two structural invariants. Still deferred: runtime cross-user authorization fuzzing, property-based/multi-tenant leakage tests, and live operator-evidence capture beyond the readiness helper.

OVE-41 adds the closed-cohort invite loop: an invited gardener arrives through an unlisted `noindex` `/join` landing page, saves a first plant entry, and returns for a same-object follow-up, all measured as privacy-safe aggregate counts. `/join` carries only the enum `invited_cohort` source into `/garden?source=invited-cohort` (`gardenFirstEntryInvitePath`), is excluded from the sitemap, and is not linked from public navigation. `invited_cohort` (ActivationSource) and `invite` (ActivationSurfaceKind) flow through the same enum-only normalizers and analytics allowlist as the existing sources, so the invite source survives the auth wall and the first save records `entry_logged` with `activation_source = invited_cohort`, which is what establishes cohort membership. `/garden/pilot-health` gains an Invited cohort loop panel (invite starts, first-entry saves, first-save rate, same-object follow-ups, returning gardeners) derived from the enum source plus aggregate same-object/membership predicates, never from raw entry text or invite identity. `docs/PRODUCTION_PILOT_SMOKE.md` documents the closed-cohort smoke steps and provisional continue/iterate/stop decision criteria grounded in `OverGarden_B2_METRICS_v0.md` and `KILL_CRITERIA_PREREG_v2.md`.

OVE-42 adds invite-gated closed-pilot writes: signed `/join?invite=` tokens set an HTTP-only eligibility cookie, materialize a durable `pilot_invite_grants` row on first authenticated write, and gate journal create paths behind `requireWriteEligibleRequestScope()`. Non-invited signed-in gardeners see a calm closed-pilot callout instead of write composers; public read routes stay open. Founders generate private invite URLs with `pnpm pilot:invite` from `apps/web` after setting `PILOT_INVITE_SIGNING_SECRET` (never commit the secret or printed links). `/garden/pilot-health` adds write-eligible gardener count from grant rows to separate invited writers from non-invited `/garden` starts.

OVE-43 adds the post-follow-up value pulse: after a successful same-object follow-up save/readback, invited gardeners see one optional private usefulness prompt (useful / not sure / not useful plus optional bounded reason or skip). Responses store as first-party `follow_up_value_pulse` analytics events with enum-only properties; journaling save/readback never blocks on feedback. `/garden/pilot-health` adds aggregate submitted/skipped/usefulness counts without journal text or identity fields.

OVE-44 adds the private two-plus-entry progress moment on the authenticated object page: returning gardeners with at least two entries on the same plant object see a compact chronological readback with dates, titles, excerpts, and derivative-only photos, including an optional earlier/latest photo comparison when both ends of the timeline have processed media. The moment stays owner-scoped and private, does not add public routes or search docs, emits enum-only `progress_screen_shown` on eligible readback, and stays hidden when fewer than two entries would make the readback misleading.

OVE-45 adds founder interview capture at `/garden/pilot-learning/interviews`: operator-only structured pilot learnings in `pilot_interview_learnings` with bounded enum fields (segment, activation result, return reason, main objection, observed value, next action, optional redacted note) and no journal text, media keys, contact details, or raw transcripts.

OVE-46 adds the pilot cohort decision readout at `/garden/pilot-learning/decision`: an operator-only panel that combines invited-cohort behavioral aggregates, follow-up value pulse distribution, structured interview category counts, explicit data gaps, and a provisional continue / iterate / stop recommendation grounded in `OverGarden_B2_METRICS_v0.md` and `KILL_CRITERIA_PREREG_v2.md`. The readout never exposes raw journal text, transcripts, emails, media keys, precise location, IP, user agent, referrer, or query strings, and it labels all metrics as provisional decision support rather than validated targets.

OVE-47 adds a non-destructive erasure dry-run preview on `/garden/privacy/erasure-requests`: for each intake row, operators see affected data classes and counts (account/auth-adjacent rows, garden workspace, journal entries, media processing rows, public slugs/tombstones, analytics events, provisional catalog rows, search/index artifacts, and pilot operator records) without selecting journal text, media keys, emails, tokens, IP addresses, user agents, referrers, or precise location. Operators can mark `dry_run_reviewed_at` on open requests as a repeatable review checkpoint; no destructive erasure or anonymization happens in this slice.

OVE-97 adds the current-schema irreversible erasure execution workflow behind the same operator gate: a request must be open, the dry-run preview must be marked reviewed, and the operator must type a request-specific maintainer approval phrase before execution. The workflow deletes Better Auth sessions/accounts/verification rows, deletes the Better Auth user row, removes pilot invite grants, deletes requester-owned analytics events and media rows, deletes OverGarden-controlled R2 quarantine/public derivative objects when their keys are still known, anonymizes/rekeys spaces, plant objects, journal entries, and erasure request subject ids to a synthetic erased subject id, deletes provisional catalog rows created by the requester, nulls catalog/interview subject links where appropriate, preserves public slug tombstones as archived/private/noindex `410` pages, removes stale requester-owned journal index jobs, and enqueues `journal_entry_unindex` jobs using the synthetic erased owner id so the worker can remove derived search documents after rekey. The workflow is current-schema only; lineage/social graph tombstone semantics remain post-MVP under `docs/LINEAGE_SCOPE_DECISION.md`.

OVE-48 adds closed-pilot auth recovery: `/garden` uses real email/password sign-in and sign-up, duplicate sign-up attempts steer gardeners back to the existing account, `/auth/help` and `/auth/reset-password` provide the operator-assisted reset path, and founders generate one-time reset URLs with `pnpm pilot:reset-password` from `apps/web` (never commit printed links). Recovery returns the gardener to the same Better Auth user id and owner-scoped garden data; no automated email delivery is promised during the closed pilot.

OVE-49 extends catalog curation with pilot-signal review: `/garden/catalog/curation` now highlights provisional names created by invited closed-pilot gardeners, surfaces aggregate-safe counts (pilot-origin flag, affected objects, invited gardener count) without journal text, media keys, emails, or precise location, and keeps confirm/merge/reject decisions on the existing path. Confirming or merging updates plant-object catalog identity only (journal entries stay put), revalidates affected public journal and variety slugs, and enqueues the public-safe catalog typeahead reindex job so provisional rows never enter global typeahead until confirmed/merged.

OVE-38 hardens and field-proofs offline journal capture with a photo on the iOS Safari engine class. The offline composers now copy a picked photo's bytes into an in-memory `Blob` at capture time (`createOfflinePhotoIntent`) so the queued photo intent is owned by IndexedDB instead of a file-backed `File` reference that WebKit can drop across reload or tab eviction (the exact offline -> reconnect -> retry window); both first-entry and same-object follow-up composers fail closed with user-facing copy if the photo cannot be read. This smallest-necessary change is covered by regression tests proving the bytes are copied rather than the `File` reference and stay readable after an IndexedDB round-trip, alongside the existing idempotent queue/retry/no-duplicate and failed-retry-retains-content-and-photo-intent tests. A closest-available mobile WebKit field-equivalent (Playwright WebKit, iPhone 13 device descriptor, `isWebKit`/`isMobile` true) confirmed on the engine that an entry plus photo intent queue while `navigator.onLine === false` and that title/body/date persist in IndexedDB. That automation build additionally exhibits two artifacts that are not iOS behavior: it cannot read an IDB-round-tripped blob's bytes back (`NotReadableError`) and it crashes on any page load against an origin that already holds IndexedDB data; both block an automated byte-level retry chain, so the byte-survival, canonical retry, and no-duplicate contract is proven deterministically in Node + fake-indexeddb instead. On 2026-06-29 the maintainer ran that required manual real-iOS-Safari field smoke on the OVE-37 pilot URL and confirmed pass (offline queue -> manual retry after reconnect -> canonical `/api/garden/entries` -> exactly one server entry with no duplicate -> derivative-only authenticated readback), so OVE-38 is closed; the procedure and redacted evidence template remain in `docs/PRODUCTION_PILOT_SMOKE.md`.

## Proven Locally

- Next.js App Router + TypeScript builds successfully.
- shadcn/ui renders inside SSR pages.
- Better Auth route is mounted at `/api/auth/[...all]`; live sign-up returns a session cookie.
- Kysely + `pg` connected to local Docker Postgres in the original walking-skeleton proof; current supported-Mac local infra starts Postgres through `infra/container-up`, with Docker retained as fallback.
- Better Auth tables are created through Better Auth's migration helper during `pnpm local:bootstrap`.
- SQL app schema creates `health`, `spaces`, `catalog_items`, `catalog_item_names`, `catalog_source_snapshots`, `catalog_source_records`, `catalog_source_links`, `plant_objects`, `journal_entries`, `analytics_events`, `media_assets`, `erasure_requests`, `variety_seed_proofs`, and `job_queue`.
- `kysely-codegen` generated `src/db/generated.ts` from 23 live tables.
- CI runs a non-mutating `pnpm db:types:check` against the bootstrapped database and fails if `src/db/generated.ts` differs from the live schema.
- `/skeleton` and `/api/skeleton/journal` prove auth -> scoped repository -> Postgres -> queue -> SSR readback.
- `/garden` and `/garden/objects/[objectId]` prove the first product path outside `/skeleton`: authenticated create/readback for one space, one plant object, and one title/body entry.
- `/garden` first-entry flow can select a seeded catalog item or explicitly keep the object Unknown; selected objects store server-validated `catalog_item_id`, `variety_state = selected`, and canonical catalog display text on the plant object.
- `/garden` first-entry flow can explicitly add a missing catalog name; the server stores it as a provisional `catalog_items` row owned by the user scope and attaches it to the plant object with `variety_state = user_added`. There is no `catalog_curation` worker queue: `/garden/catalog/curation` reads provisional `catalog_items` directly, and confirm/merge/reject decisions enqueue only the consumed `matching` / `catalog_typeahead_reindex` job.
- `/api/garden/catalog/typeahead` provides a signed-in, bounded catalog suggestion path. It tries the derived Meilisearch `catalog_typeahead` index first and falls back to the Postgres seeded/confirmed query when search is unavailable.
- Catalog typeahead Meili documents contain only catalog item ID, display/canonical names, normalized alias text, catalog kind, locale hints, status/source, and ranking fields. They exclude owner IDs, private journal text, precise location, media metadata, analytics payloads, email, IP, user agent, raw source fields, external source IDs, breed validation-only fields, BG/EU parser/legal/source-row metadata, and alias curation metadata.
- Provisional user-added catalog rows remain out of the global typeahead index until a later curation path promotes safe public identity.
- `/garden/objects/[objectId]` lets a signed-in owner resolve an existing Unknown or user-added object to a seeded/confirmed catalog item via the same app API typeahead, without moving, duplicating, or rewriting existing journal entries.
- Catalog resolution is object-level only: the server validates the selected catalog item, scopes the object update to the current owner, permits only Unknown/user-added -> selected transitions, revalidates affected public SSR entry paths, and leaves public entries `noindex`.
- `/garden/catalog/curation` provides a minimal internal scaffold for provisional catalog names. It shows only candidate display name, locale/status/source, created date, and aggregate object count; it does not show journal title/body, precise location, media metadata, email, IP, user agent, or private media URLs.
- Catalog curation can confirm a provisional row into a global `confirmed` catalog item, merge it into an existing seeded/confirmed item while moving affected objects to `selected`, or reject it without making it canonical. Confirm/merge enqueue the existing catalog typeahead reindex job.
- Seeded and confirmed global catalog rows can have a stable ASCII `public_slug`. Seeded rows are assigned deterministic seed slugs; confirmed user-added rows receive a server-generated slug only when curation clears owner scope. Provisional/user-owned rows do not become public variety identities.
- `/garden` first-entry flow can attach one processed photo asset and `/garden/objects/[objectId]` renders only the stripped public derivative.
- `/garden` first-entry flow can queue title/body/date/object/photo intent while offline, show queued/syncing/failed/synced local states, retry through `/api/garden/entries`, and read back exactly one authenticated server entry.
- `/garden/objects/[objectId]` can publish an existing entry through a signed-in server action after explicit disclosure, and `/journal/[slug]` renders a public SSR page that stays `noindex`.
- `/journal/[slug]` links to `/variety/[slug]` only when the entry's object has selected seeded/confirmed global catalog identity with a public slug.
- `/variety/[slug]` renders an SSR public aggregation page for a seeded/confirmed catalog item only when at least one safe public entry exists. It aggregates only active `visibility = public`, non-gone entries whose object is `selected`, whose catalog row is not owner-scoped, and whose media is already processed derivative media.
- `/variety/[slug]` now computes a server-side indexability state from the same safe public aggregation read model. The first conservative threshold is at least 3 active public entries and at least 600 aggregate journal body characters. Thin pages stay `noindex, nofollow`; threshold-passing pages become `index, follow`.
- `/sitemap.xml` includes only threshold-passing public variety pages. The sitemap query repeats the same public-safe filters and excludes archived, public-gone, private, provisional, owner-scoped, unknown, and below-threshold rows.
- Public variety JSON-LD is emitted only for threshold-passing pages and is limited to CollectionPage/WebSite/Thing plus entry headline/date/public URL. It does not include body text, precise location, owner fields, media storage keys, quarantine keys, or raw analytics fields.
- Public variety pages render required source credits for imported catalog facts from a dedicated safe read model. Credits include source name/version/source URL/license/license URL/attribution text only; JSON-LD and Meilisearch/typeahead documents do not carry source credits, raw source payloads, source-only fields, external source IDs, occurrence/distribution coordinates, checksums, or source record keys.
- `/garden/catalog/curation` can create or update one plain-text founder seed proof block for a seeded/confirmed public catalog item. Published proof blocks render on that item's `/variety/[slug]` page beside safe UGC; draft proof blocks do not render publicly.
- Seed proof blocks are intentionally not a CMS: one block per catalog item, no rich HTML, no media library, no bulk creation, and no automatic page spawning. Server validation rejects provisional/user-owned catalog rows, unsafe raw HTML, and obvious precise-location/private field keys.
- `/variety/[slug]` has a direct activation CTA into `/garden` that carries only the public catalog slug plus an enum source. `/garden` resolves that slug server-side to a seeded/confirmed global catalog item before passing a preselected catalog match to the first-entry composer.
- Signed-out visitors keep the same activation URL through the local Better Auth panel. After sign-up/sign-in, the existing `/garden` first-entry flow opens with the catalog item preselected; signed-in visitors land directly in that state. Users can still clear the selection, choose another catalog item, add a missing name, or keep Unknown.
- `/garden` now prioritizes continuation when the signed-in gardener already has plant objects: existing objects are shown as the primary workspace path, while first-entry creation remains available as a secondary path.
- `/garden/objects/[objectId]` now supports existing-object follow-up entries with title/body/date plus one optional photo intent, online submit or offline queue/retry, canonical `/api/garden/entries` sync, idempotent same-object creation, derivative-only media attachment, and authenticated readback.
- `/garden`, `/garden/objects/[objectId]`, and the first-publication/privacy notices now keep the pilot user-facing copy out of scaffold/implementation language. Offline recovery is described as entries saved on this device, catalog state is described as matched / user-provided / no match yet, public photos are described as server-cleaned copies, and pilot public pages are described as not listed for search engines rather than by raw implementation terms.
- `/health` remains public for uptime and manual smoke checks, but it is explicitly `noindex, nofollow`, excluded from the sitemap, and limited to bounded infrastructure status. It must not render raw request metadata or private user data.
- `/` now acts as a pilot acquisition surface, not a scaffold dead end: its primary CTA opens `/garden?source=homepage`, keeping homepage intent through the local Better Auth wall into the same first-entry composer and canonical create/readback path.
- `/join` is an unlisted `noindex` closed-cohort invite landing page for invited gardeners. It uses calm, non-technical copy and its only forward action opens `/garden?source=invited-cohort`, keeping the enum `invited_cohort` intent through the Better Auth wall into the same first-entry composer. It is excluded from the sitemap and not linked from public navigation, carrying no invite token, name, or email.
- First-entry activation attribution is bounded to enum-only `activation_source` and `source_surface_kind` values for homepage, public variety, and direct garden starts. Public-variety attribution is used only when the catalog slug resolves server-side to a selectable seeded/confirmed global catalog item.
- `/erasure` now provides authenticated erasure request intake plus latest-status readback. It records only requester user id, request scope/status/timestamps, handler status fields, and the intake disclosure version; the form itself does not automatically delete or anonymize product data.
- `/garden/privacy/erasure-requests` provides an authenticated operator review surface through the shared admin role/capability gate. The read model and non-destructive status actions intentionally exclude journal title/body, media keys, raw request metadata, precise location, email, IP, user agent, and referrer. Irreversible execution is a separate owner/admin-only maintainer-approved action that requires prior dry-run review and a request-specific approval phrase.
- `/privacy` and `/first-publication-disclosure` expose closed-pilot reviewed copy status, versioned disclosure constants, explicit public-release blockers, and noindex metadata instead of generic scaffold text that could be mistaken for release-ready legal copy.
- `/garden/pilot-health` provides an authenticated operator readout for provisional H1/H4/H6 leading indicators: first-entry activations, same-object follow-ups, photo usage, offline queued/synced state, publish/archive/410 counts, homepage/public-variety/direct/invited-cohort activation starts and saves, the invited-cohort loop (invite starts, first-entry saves, first-save rate, same-object follow-ups, returning gardeners), and public-variety promoted/thin/de-promoted counts.
- Pilot health intentionally reports only aggregate counts/rates and provisional labels. It excludes journal title/body text, precise location, raw event properties, emails, IP/user-agent/referrer/query values, and media storage keys from the read model. Offline failed mutations remain client-local Dexie state and are labeled as not server-observable rather than treated as a healthy zero.
- `/garden/pilot-smoke` provides a role-gated operator readiness readout for production/preview smoke. It reports configured/missing/placeholder state for public URL, auth URL, admin-role bootstrap, database, R2, Meilisearch, matching service, and Vercel runtime without echoing secrets or operator user IDs. It also carries the manual smoke sequence and evidence redaction rules for auth -> journal -> media derivative -> public SSR -> 410 -> activation -> pilot-health verification.
- Pilot smoke intentionally marks public journal search worker health as degraded until a live run proves `journal_entry_index` / `journal_entry_unindex` jobs are processed. The current Python worker still proves `catalog_typeahead_reindex` only.
- `/garden/objects/[objectId]` can archive a published entry privately; the old `/journal/[slug]` public URL returns HTTP `410 Gone` and search document conversion refuses archived/public-gone rows.
- `/garden/objects/[objectId]` can emit an owner-scoped revisit event, show prior entries, and append another dated title/body entry to the same plant object.
- `/garden/objects/[objectId]` shows a private two-plus-entry progress moment with chronological dates/titles/excerpts and derivative-only media; fewer than two entries keep the moment hidden.
- `/garden` and `/garden/objects/[objectId]` now use a controlled coarse-region contract for UA/BG ISO 3166-2 subdivision codes. Default visibility remains `hidden`; public `/journal/[slug]` renders a region label only when the object is explicitly `region` visible.
- Product writes emit first-party event rows for activation starts, entry saves, photo attachment, offline sync, progress readback, and same-object return loops without title/body text, precise location, raw media metadata, email, public URL/referrer/query, user agent, or IP-derived location in event properties.
- Public-variety activation attribution is intentionally enum-only: `activation_source = public_variety` and `source_surface_kind = variety`. It never stores the public URL, catalog query string, referrer, selected display text, or journal title/body.
- Catalog selection event properties remain enum-only through `variety_state`; raw catalog query text and selected display strings are not analytics properties.
- Catalog repository contract tests prove typeahead reads only safe catalog tables, selectable IDs and public activation slugs are limited to seeded/confirmed global rows, Meili hits are deduped and filtered, reindex rows exclude owner-scoped catalog items, provisional candidates are upserted by owner/normalized name/locale, and curation/reindex job payloads exclude journal title/body content.
- Journal repository contract tests prove catalog resolution is owner-scoped, limited to Unknown/user-added states, does not update `journal_entries`, and revalidates only active public slugs without selecting private entry fields.
- Catalog curation repository tests prove pending lists expose aggregate-safe metadata only, confirm clears owner scope and makes the item index-eligible, merge updates affected objects without moving entries, reject keeps candidates non-canonical, and public revalidation queries avoid private entry fields.
- Public variety repository tests prove aggregation is limited to global seeded/confirmed catalog rows, selected objects, active public non-gone entries, owner-consistent entry/object/space joins, and derivative-only media selection without quarantine keys, raw media metadata, precise location, or owner-private fields in the read model.
- Media owner-boundary tests prove the upload API rejects direct `journalEntryId` pre-binding, processed media attachment requires target entry ownership, public journal media joins require owner equality, and public variety summary/readback excludes owner-mismatched derivatives.
- Public variety indexing tests prove thin pages remain out of index/sitemap until both the entry-count and aggregate-text thresholds pass, and metadata tests prove JSON-LD stays bounded to public-safe fields.
- Variety seed proof tests prove proof blocks attach only to global seeded/confirmed catalog rows with public slugs, public readback selects only published proof fields, sitemap/indexability does not depend on seed proofs, and validation rejects raw HTML plus obvious precise-location/private keys.
- Repository contract tests prove owner-scoped object readback and idempotent entry creation through `(owner_user_id, client_mutation_id)`.
- Analytics event tests prove event payload allowlists, enum-only homepage/public-variety/direct activation attribution, rejection of raw URL/referrer/query/user-agent fields, owner/session/object linkage, same-session revisit follow-up marking, and non-blocking event failure logging.
- Pilot health tests prove operator access gating, safe aggregate SQL for journal/event/public-variety readouts, public-variety promoted/thin/de-promoted mapping, and non-blocking readout failure handling.
- Pilot smoke readiness tests prove secret values are not emitted in the operator readout, local placeholder or local-fallback auth config blocks deployed smoke, and journal search indexing remains explicitly degraded until worker proof exists.
- Erasure request tests prove the intake and operator readback queries use only bounded request metadata, do not join journal/media/auth-session tables, and reject raw content/location/request-header fields from the read model. Access tests prove unauthenticated operator readback resolves to a sign-in-required state.
- Repository contract tests prove owner-scoped publication, first-publication disclosure lookup, public slug readback, and derivative-only public media selection.
- Repository contract tests prove owner-scoped archive, public-gone tombstone lookup, active-only public readback, and active-only derivative media selection.
- R2 quarantine upload and public derivative processing work against the configured Cloudflare R2 buckets and `media.over.garden`.
- Media processor tests prove the quarantine original is deleted before the public derivative is written.
- `sharp` derivative tests prove WebP output without EXIF metadata.
- Dexie offline queue is test-covered with IndexedDB shim.
- Search document privacy tests prove private/unpublished entries are not indexed and public documents exclude owner-private fields, quarantine keys, precise location, and rows without public slugs.
- Catalog search document tests prove the `catalog_typeahead` document shape excludes provisional/user-owned rows and rejects unsafe Meili hits before they can appear in the picker.
- Coarse-region contract tests prove supported UA/BG subdivision codes normalize predictably, free-form/precise-location strings are rejected, and the SQL schema does not add exact-location columns.
- An integrated OVE-40 privacy invariant sweep (`apps/web/test/privacy/`) threads one poisoned user journey through public journal SSR HTML, journal/catalog search document conversion, public variety JSON-LD, analytics event properties, the operator erasure list query, the pilot-health aggregate readout, and the pilot smoke readiness evidence, asserting no forbidden field name or value leaks, hidden-vs-region visibility, derivative-only media, user-markup escaping, and archived/public-gone search exclusion. The scanner is mutation-verified: a deliberate renderer leak fails the sweep.
- Static OVE-40 schema invariants assert the SQL schema and generated Kysely types contain no precise-location columns, keep the UA/BG coarse-region controlled vocabulary, separate quarantine vs public derivative media keys, and bound analytics properties to a jsonb object.
- Python worker consumes the Postgres-backed queue and marks jobs `done`.
- Worker restart/recovery is proven deterministically: stale `processing` jobs are reclaimed only after the visibility timeout, `journal_entry_index`/`journal_entry_unindex` reach `done` after a simulated restart, the public-safe document contract holds, re-delivery is idempotent, and a transient Meilisearch outage fails-then-recovers (`services/matching/tests/test_worker_recovery.py`).
- Meilisearch Cyrillic typo proof passed against local Docker Meilisearch in the original scaffold; current supported-Mac local infra starts Meilisearch through `infra/container-up` while preserving the same `app.search` catalog-specific Cyrillic typo proof for the `catalog_typeahead` index.

## External Infra Provisioned

- Cloudflare zone `over.garden` is active under account `cb03b15042adc74edfe2d8201636300a`.
- R2 buckets `overgarden-quarantine` and `overgarden-public` exist.
- R2 custom domain `media.over.garden` is attached to `overgarden-public` with ownership and SSL active.
- Vercel deployment env has the R2 runtime env family, `BETTER_AUTH_SECRET`, and production `PILOT_INVITE_SIGNING_SECRET` installed. Secret values remain outside git.
- DigitalOcean Managed PostgreSQL cluster `overgarden-postgres-prod-fra1` exists in `FRA1`; Vercel production and active OVE-27 branch preview have canonical `DATABASE_URL`, `DIRECT_URL`, `DATABASE_SSL=true`, and `DATABASE_SSL_CA` installed.
- Production database bootstrap has run against the managed cluster, creating the app schema and Better Auth tables. OVE-51 reran the non-destructive bootstrap and confirmed `pilot_invite_grants` exists for invite-gated writes.
- The historical OVE-27 branch preview had branch-specific `PUBLIC_SITE_URL` / `BETTER_AUTH_URL` and a matching temporary R2 quarantine CORS origin for browser upload smoke.
- On 2026-06-27, the OVE-27 branch preview passed homepage first-entry with photo, derivative-only readback, same-object follow-up, public SSR journal page, public variety activation, archive-to-410, and authenticated pilot-health readout against the managed database and R2.
- Vercel project `over-garden` exists under team `team_vs3oQAk6OT4vVVvcL7Mf5m8t`; `over.garden` and `www.over.garden` are attached and DNS-resolve to Vercel. Production `PUBLIC_SITE_URL` and `BETTER_AUTH_URL` are canonicalized to `https://over.garden`, which is now the selected public pilot URL.
- Live non-secret infrastructure values are recorded in `docs/INFRASTRUCTURE_REGISTRY.md`.

## Product Research Imported

- The original Startups research folder is duplicated into `docs/product-research/`.
- `docs/product-research/README.md` defines the Product Thinking Gate and routes agents to relevant research by task type.

## H1-Safe Event Instrumentation

- Event rows are first-party provisional measurement scaffolding, not final PostHog integration, dashboards, or validated targets.
- Event targets and thresholds remain provisional until live pilot calibration. Current numbers in the research corpus are pilot calibrators or desk thresholds, not confirmed OverGarden success criteria.
- `activation_started` is a bounded server-side intent event for `/garden` starts from homepage, public-variety, direct garden, or invited-cohort sources. It stores only enum attribution and never stores raw URLs, referrers, query strings, selected display text, or journal content.
- `own_record_revisited` is diagnostic until followed by a later `entry_logged` on the same object in the same session. The linkage is stored through event relation/state, not by adding raw entry content to properties.
- Event properties are limited to booleans and enums such as `entry_scope`, `has_photo`, `is_backdated`, `location_visibility_level`, `sync_status`, `variety_state`, and `followed_by_action`.
- Do not add title, body, exact location, EXIF, raw media metadata, email, IP, user agent, or address fields to analytics properties.

## Admin Role-Gated Operator Surfaces

- `/admin` and existing operator surfaces use durable Better Auth user roles in `admin_user_roles`.
- `/admin/users` is owner-only role management with durable audit rows in `admin_role_audit_log`.
- `/garden/catalog/curation` requires `operator:mutate`; pilot health/smoke/decision readouts allow `operator:read`; founder interview capture requires `operator:mutate`; erasure execution requires `erasure:execute`.
- The legacy `CATALOG_CURATOR_USER_IDS` allowlist pattern is deprecated and must not be used for new operator surfaces.
- The curation surface is intentionally narrow: pending provisional names, aggregate affected-object counts, confirm, merge, source candidate review, and reject. It is not a public moderation product.

## Conservative Region Decision

- Current implementation uses a conservative controlled vocabulary: ISO 3166-2 subdivision codes for Ukraine and Bulgaria only, exposed through `coarse_region_code`.
- `hidden` is still the default. When the current create/edit forms submit `hidden`, the server stores `coarse_region_code = null` instead of retaining a private region value. This minimizes stored location data for v0.
- Revisit before public variety-region pages or launch localization: whether hidden objects should retain a private coarse region for later toggles, whether labels should be localized, and whether any climate-zone grouping should sit beside the ISO code rather than replace it.

## Closed-Pilot Privacy Copy Decision

- GDPR/privacy, erasure-request, and first-publication disclosure copy is reviewed for the closed pilot and no longer uses placeholder language. It is still not public-launch legal approval.
- Public release remains blocked until final legal policy text, verified operator contact/response process, processor/retention/legal-basis wording, and production proof of the maintainer-approved erasure workflow are complete.
- Routes `/privacy`, `/erasure`, `/first-publication-disclosure`, and `/garden/privacy/erasure-requests` remain `noindex`.

## Conservative Public Variety Decision

- Public variety pages remain `noindex` by default unless they pass the centralized OVE-19 threshold: at least 3 active public entries and at least 600 aggregate journal body characters.
- This is intentionally conservative and likely too simple for the long term. Revisit after pilot data: minimum photo diversity, region distribution, localized slug/transliteration strategy, and whether variety-region pages need stricter k-anonymity-like gates.
- Photo count is not part of the first promotion threshold. Reason: early entries may be useful without enough media coverage, and media quality/diversity should be calibrated from real UGC rather than guessed before OVE-21's richer seeded SEO surface exists.
- Founder seed proof is not part of the first promotion threshold and cannot make a page indexable by itself. Reason: seed proof is a cold-start quality aid, not proof of UGC density; using it as a promotion shortcut would recreate thin editorial doorway-page risk.
- If a catalog slug has zero safe public entries, `/variety/[slug]` returns 404 instead of an empty indexable shell.

## Verification Commands

The Apple Container start below is the primary supported-Mac local runtime path. Docker Compose remains documented in `infra/README.md` only as fallback.

```bash
infra/container-up
infra/container-status
cd apps/web
cp .env.example .env.local
pnpm mainline:closeout:check
pnpm local:bootstrap
pnpm db:types
pnpm db:types:check
pnpm lint
pnpm typecheck
pnpm test
BETTER_AUTH_SECRET="$(openssl rand -base64 32)" pnpm build
cd ../../services/matching
container build -t overgarden/matching:local .
uv run python -m py_compile app/main.py app/search.py app/worker.py
uv run --frozen pytest
MEILISEARCH_HOST='http://localhost:7700' MEILISEARCH_API_KEY='local_dev_meili_master_key_change_me_1234567890' uv run python -m app.search
```

## Still Deferred

- Fresh canonical-domain production media derivative readback after any app/env deployment that changes the media/auth path.
- Public journal search worker handling for `journal_entry_index` and `journal_entry_unindex` is live-proven (OVE-36 canary) and recovery-proven after restart (OVE-39); a fresh live run is still required after any worker, env, or search change.
- OAuth providers and full automated email deliverability beyond the closed-pilot operator-assisted reset path (OVE-48).
- Public-release legal policy, verified operator contact process, and production proof of the maintainer-approved erasure workflow before public self-serve traffic.
- Runtime cross-user authorization fuzzing and property-based/multi-tenant leakage tests beyond the OVE-40 integrated single-journey sweep and the existing repository contract tests.
- Production-grade admin roles and audit UI beyond the minimal catalog curation scaffold.
- PostHog integration, analytics dashboards, and full cohort reporting.

## Next Build Step

`SDD Slice 9 - Catalog Source Ingestion And Canonical Seed` is complete across OVE-55 through OVE-64. Before starting the next Linear issue, read `docs/MAINLINE_CLOSEOUT.md`, run `cd apps/web && pnpm mainline:closeout:check`, then pull the next active Linear queue item rather than inferring it from historical roadmap text. Future source work must keep the OVE-55 gate active: conditional/internal-only sources, blocked PGR sources, and vendor/marketplace paths cannot feed canonical projection until their source-specific blockers are closed.

Every future Linear issue must be a vertical SDD slice, not a layer ticket. It must start from a user behavior and integrate the needed surfaces together: SQL/types -> scoped repository -> route/action/API -> UI -> background job/search/media/offline/event boundary when relevant -> tests -> docs. Do not create standalone tasks for "schema", "UI", "media", "analytics", "search", or "public pages" unless that work is inside the same issue as the user-visible path.

The completed catalog-select and user-added fallback slices must not be split into standalone schema, typeahead UI, search, or repository follow-ups unless a later user-visible path requires them.

Before implementing the next issue, run the Product Thinking Gate in `docs/product-research/README.md` and include the selected research files in the Linear context.
