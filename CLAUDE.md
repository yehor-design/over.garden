# CLAUDE.md

The authoritative agent guidance for this repository is `AGENTS.md` — Claude Code reads it. Read `AGENTS.md` in full before any change, and read the relevant record in `docs/adr/` and `docs/TECH_STACK_DECISIONS.md` before touching the area it governs.

This file exists only to point Claude Code at `AGENTS.md`, so the guidance has a single source of truth and does not drift across files. Put Claude-specific or personal overrides in `CLAUDE.local.md` (git-ignored). Never put secrets in any of these files.
